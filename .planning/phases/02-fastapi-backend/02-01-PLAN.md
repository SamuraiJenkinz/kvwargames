---
phase: 02-fastapi-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/__init__.py
  - backend/app/config.py
  - backend/app/dependencies.py
  - backend/app/main.py
  - backend/app/routers/__init__.py
  - backend/app/routers/llm.py
  - backend/app/routers/config_gen.py
  - backend/requirements.txt

must_haves:
  truths:
    - "Running `pip install -r backend/requirements.txt` installs fastapi, httpx, and pydantic-settings without errors"
    - "Settings class reads LLM_API_KEY, LLM_ENDPOINT_URL, LLM_MODEL from environment and raises ValidationError if any are missing"
    - "Settings class has defaults for LLM_TIMEOUT_SECONDS (60), LLM_MAX_TOKENS (2048), LLM_EXTRA_HEADERS ('{}')"
    - "FastAPI app starts with lifespan context manager that validates settings and creates httpx.AsyncClient"
    - "All backend Python files import without errors"
  artifacts:
    - path: "backend/app/config.py"
      provides: "pydantic-settings Settings class with all LLM env vars"
      contains: "class Settings"
      exports: ["Settings", "get_settings"]
    - path: "backend/app/main.py"
      provides: "FastAPI app instance with lifespan, router mounts, validation error handler"
      contains: "app = FastAPI"
    - path: "backend/app/dependencies.py"
      provides: "FastAPI dependency injection helpers"
      contains: "get_http_client"
    - path: "backend/requirements.txt"
      provides: "Python dependencies"
      contains: "fastapi"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/config.py"
      via: "imports get_settings for lifespan validation"
      pattern: "from .config import get_settings"
    - from: "backend/app/main.py"
      to: "backend/app/routers/llm.py"
      via: "includes router"
      pattern: "app.include_router"
autonomous: true
---

<objective>
Create the FastAPI project skeleton: directory structure, pydantic-settings configuration, lifespan with httpx client, dependency injection helpers, empty router stubs, and requirements.txt.

Purpose: This is the backend foundation. All subsequent plans (LLM proxy, config gen, static serving) build on this app structure. Settings validation ensures fast startup failure on missing env vars.

Output: A FastAPI app that starts (with valid env vars), validates configuration, creates a shared httpx client, and has empty router stubs mounted at the correct paths.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\KVWarGame\.planning\PROJECT.md
@C:\KVWarGame\.planning\ROADMAP.md
@C:\KVWarGame\.planning\STATE.md
@C:\KVWarGame\.planning\phases\02-fastapi-backend\02-CONTEXT.md
@C:\KVWarGame\.planning\phases\02-fastapi-backend\02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend directory structure, requirements.txt, and config.py</name>
  <files>
    backend/app/__init__.py
    backend/app/config.py
    backend/app/routers/__init__.py
    backend/requirements.txt
  </files>
  <action>
    1. Create directory structure:
       ```
       backend/
       ├── app/
       │   ├── __init__.py          (empty)
       │   ├── routers/
       │   │   └── __init__.py      (empty)
       └── requirements.txt
       ```

    2. Create `backend/requirements.txt`:
       ```
       fastapi[standard]>=0.135.0
       httpx>=0.28.0
       pydantic-settings>=2.13.0
       ```

    3. Install dependencies: `pip install -r backend/requirements.txt`

    4. Create `backend/app/config.py` with pydantic-settings:
       - Import `BaseSettings`, `SettingsConfigDict` from `pydantic_settings`
       - Import `lru_cache` from `functools`, `json` from stdlib
       - Class `Settings(BaseSettings)`:
         - `model_config = SettingsConfigDict(env_file=".env", extra="ignore")`
         - Required fields (no default = startup fails if missing):
           - `llm_api_key: str`
           - `llm_endpoint_url: str`
           - `llm_model: str`
         - Optional fields with defaults:
           - `llm_timeout_seconds: int = 60`
           - `llm_max_tokens: int = 2048`
           - `llm_extra_headers: str = "{}"`
         - Method `get_extra_headers(self) -> dict`: parse `llm_extra_headers` with `json.loads()`, wrap in try/except returning `{}` on `json.JSONDecodeError`
       - Function `get_settings() -> Settings` decorated with `@lru_cache`

    CRITICAL: pydantic-settings field names are lowercase. `llm_api_key` reads from env var `LLM_API_KEY` automatically.
    AVOID: Do NOT parse `LLM_EXTRA_HEADERS` in `__init__` or a validator — parse lazily in `get_extra_headers()`.
  </action>
  <verify>
    Run `pip install -r backend/requirements.txt` — installs without errors.
    Run `python -c "from backend.app.config import Settings, get_settings; print('config OK')"` from project root — no import errors.
    Run `python -c "from backend.app.config import Settings; s = Settings(llm_api_key='k', llm_endpoint_url='u', llm_model='m'); print(s.llm_timeout_seconds)"` — prints 60.
  </verify>
  <done>
    backend/ directory exists with correct structure. requirements.txt installs cleanly. Settings class validates required env vars and provides defaults for optional ones. get_extra_headers() parses JSON safely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create main.py with lifespan, router stubs, validation error handler, and dependencies.py</name>
  <files>
    backend/app/main.py
    backend/app/dependencies.py
    backend/app/routers/llm.py
    backend/app/routers/config_gen.py
  </files>
  <action>
    1. Create `backend/app/dependencies.py`:
       - Function `get_http_client(request: Request) -> httpx.AsyncClient`: returns `request.app.state.http_client`
       - Import `Request` from `fastapi`, `httpx`

    2. Create `backend/app/routers/llm.py`:
       - `router = APIRouter()` (import from fastapi)
       - Empty for now — Plan 02-02 will add the endpoint

    3. Create `backend/app/routers/config_gen.py`:
       - `router = APIRouter()` (import from fastapi)
       - Empty for now — Plan 02-03 will add the endpoint

    4. Create `backend/app/main.py`:
       - Imports: `contextlib.asynccontextmanager`, `httpx`, `FastAPI`, `RequestValidationError` from `fastapi.exceptions`, `JSONResponse` from `fastapi.responses`
       - Import `get_settings` from `.config`
       - Import `llm` and `config_gen` from `.routers`
       - Define `lifespan` async context manager:
         - Call `get_settings()` — raises `ValidationError` if required env vars missing, preventing startup
         - Create `httpx.AsyncClient(timeout=httpx.Timeout(settings.llm_timeout_seconds))`
         - Store on `app.state.http_client`
         - `yield`
         - `await app.state.http_client.aclose()`
       - Create `app = FastAPI(lifespan=lifespan)`
       - Add `RequestValidationError` exception handler that returns 400 with `{"error": {"code": "VALIDATION_ERROR", "message": "Malformed request body"}}` instead of FastAPI's default 422
       - Mount routers: `app.include_router(llm.router)`, `app.include_router(config_gen.router)`
       - Do NOT mount static files yet — that's Plan 02-04

    CRITICAL: Router includes MUST come before any future static mount. The current plan leaves a comment: `# SPA static files mounted here — see Plan 02-04`
    AVOID: Do NOT use `@app.on_event("startup")` — it is deprecated. Use lifespan only.
    AVOID: Do NOT add CORS middleware — Vite proxy handles dev, and production serves from same origin.
  </action>
  <verify>
    Set test env vars and start the app:
    ```bash
    cd C:/KVWarGame
    LLM_API_KEY=test LLM_ENDPOINT_URL=http://localhost LLM_MODEL=test python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 &
    sleep 2
    curl -s http://127.0.0.1:8000/docs | head -20
    ```
    The /docs page should return HTML (FastAPI auto-docs). Kill the server after.

    Verify missing env vars cause startup failure:
    ```bash
    python -c "from backend.app.config import Settings; Settings()" 2>&1 | grep -i "validation"
    ```
    Should show ValidationError mentioning missing fields.
  </verify>
  <done>
    FastAPI app starts with valid env vars. Lifespan validates settings and creates httpx client. RequestValidationError returns 400 instead of 422. Router stubs are mounted. Missing env vars prevent startup with clear error.
  </done>
</task>

</tasks>

<verification>
1. `pip install -r backend/requirements.txt` — clean install
2. `python -c "from backend.app.config import Settings; Settings(llm_api_key='k', llm_endpoint_url='u', llm_model='m')"` — no error
3. `python -c "from backend.app.config import Settings; Settings()"` — raises ValidationError
4. With env vars set: `python -m uvicorn backend.app.main:app` starts on port 8000
5. `curl http://127.0.0.1:8000/docs` returns HTML
6. `grep "lifespan" backend/app/main.py` — lifespan pattern used
7. `grep "on_event" backend/app/main.py` — should return nothing (deprecated pattern not used)
</verification>

<success_criteria>
- backend/ directory structure matches the research spec
- requirements.txt installs fastapi[standard], httpx, pydantic-settings
- Settings class validates required env vars at instantiation
- FastAPI app uses lifespan context manager (not deprecated on_event)
- httpx.AsyncClient created as singleton on app.state
- RequestValidationError overridden to return 400 with consistent error shape
- Router stubs mounted (empty, ready for Plans 02-02 and 02-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-fastapi-backend/02-01-SUMMARY.md`
</output>
