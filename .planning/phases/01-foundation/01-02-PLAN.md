---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/edipConfig.ts
  - src/data/edipConfig.test.ts
autonomous: true

must_haves:
  truths:
    - "EDIP_CONFIG constant satisfies the GameConfig interface with zero TypeScript errors"
    - "Config has exactly 2 scenarios, 4 teams, 11 cards, and 4 national actions"
    - "Scenario S1 has 4 rounds; Scenario S2 has 5 rounds"
    - "Team A starts with pc=3, po=0, readiness=3, stock=2, crm=2, ic=2"
    - "All guide text fields (objective, redLines, pcThresholds, votingRule, eoMechanic, resourceLogic, facilitation) are non-empty strings"
  artifacts:
    - path: "src/data/edipConfig.ts"
      provides: "Canonical EDIP game configuration constant"
      exports: ["EDIP_CONFIG"]
      min_lines: 150
    - path: "src/data/edipConfig.test.ts"
      provides: "Runtime validation tests for EDIP config data integrity"
      contains: "describe.*EDIP_CONFIG"
  key_links:
    - from: "src/data/edipConfig.ts"
      to: "src/types/game.ts"
      via: "import GameConfig type"
      pattern: "import.*GameConfig.*from.*@/types/game"
---

<objective>
Transcribe the complete EDIP game configuration from the spec into a typed TypeScript constant, and write Vitest tests that validate data integrity at runtime.

Purpose: The EDIP config is the sole source of game data for the entire application. Every component that displays cards, teams, scenarios, or actions imports from this constant. Data errors here propagate silently to every downstream consumer. Tests catch transcription errors before they become bugs.

Output: `src/data/edipConfig.ts` exporting `EDIP_CONFIG` constant, and `src/data/edipConfig.test.ts` with structural validation tests.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\KVWarGame\.planning\PROJECT.md
@C:\KVWarGame\.planning\ROADMAP.md
@C:\KVWarGame\.planning\phases\01-foundation\01-CONTEXT.md
@C:\KVWarGame\.planning\phases\01-foundation\01-RESEARCH.md
@C:\KVWarGame\WARGAME_ENGINE_DEV_SPEC.md (Section 7 — lines 377-578 are the AUTHORITATIVE source for all EDIP data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe EDIP config constant from spec Section 7</name>
  <files>src/data/edipConfig.ts</files>
  <action>
    Create src/data/edipConfig.ts that exports `EDIP_CONFIG` as a `const` satisfying the `GameConfig` type.

    CRITICAL: Transcribe VERBATIM from WARGAME_ENGINE_DEV_SPEC.md Section 7 (lines 383-578). The spec is the authoritative source. Do not paraphrase, correct, or modify any game data text.

    The constant must include:
    - `name`: "EDIP Security of Supply Wargame"
    - `domain`: "European Defence Technological and Industrial Base (EDTIB)"
    - `description`: Full paragraph from spec
    - `scenarios`: Array of 2 — S1 (4 rounds, 4 injects) and S2 (5 rounds, 5 injects). Each inject is a full paragraph with ROUND heading, description, and KEY TENSION.
    - `teams`: Array of 4 — A, B, C, D. Each with full description, 2 persona strings, uniqueAction text, and 6 starting resource values.
    - `nationalActions`: Array of 4 — NA-1 through NA-4. Each with full summary and cost text.
    - `cards`: Array of 11 — CS-01, CS-02, MP-01, MP-02, SP-01, SP-02, CP-01, PA-01, PA-02, PA-03, TR-01. Each with cat, timing, req, and effect text.
    - `objective`: Full paragraph
    - `redLines`: Full paragraph
    - `pcThresholds`: Full paragraph
    - `votingRule`: Full paragraph
    - `eoMechanic`: Full paragraph
    - `resourceLogic`: Full paragraph
    - `facilitation`: Full paragraph

    Use `import type { GameConfig } from '@/types/game'` at the top.

    Add `as const satisfies GameConfig` for maximum type safety (compile-time validation that the constant matches the interface).

    AVOID:
    - Do NOT abbreviate or truncate any text field — copy full paragraphs from the spec
    - Do NOT change team resource starting values
    - Do NOT reorder cards, teams, or scenarios
    - Do NOT add fields not in GameConfig interface
    - Do NOT use `any` type
  </action>
  <verify>
    Run `pnpm exec tsc --noEmit` — zero errors (proves constant satisfies GameConfig).
    Visually spot-check: Team A pc=3, Team B ic=5, Scenario S1 rounds=4, S2 rounds=5, cards array has 11 entries.
  </verify>
  <done>
    EDIP_CONFIG constant exists in src/data/edipConfig.ts, compiles with zero TypeScript errors, and contains all game data verbatim from spec Section 7.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Vitest tests validating EDIP config data integrity</name>
  <files>src/data/edipConfig.test.ts</files>
  <action>
    Create src/data/edipConfig.test.ts with structural and spot-check tests.

    Tests to include:

    1. **Structural counts:**
       - `EDIP_CONFIG.scenarios` has length 2
       - `EDIP_CONFIG.teams` has length 4
       - `EDIP_CONFIG.cards` has length 11
       - `EDIP_CONFIG.nationalActions` has length 4

    2. **Scenario validation:**
       - S1 (id "S1") has rounds === 4 and injects.length === 4
       - S2 (id "S2") has rounds === 5 and injects.length === 5
       - S1 startState: crisisSeverity === 0, crisisState === "No Crisis", edipLegitimacy === 0
       - S2 startState: crisisSeverity === 0, crisisState === "No Crisis", edipLegitimacy === 0

    3. **Team starting values (spot-check all 4 teams):**
       - Team A: pc=3, po=0, readiness=3, stock=2, crm=2, ic=2
       - Team B: pc=4, po=1, readiness=3, stock=3, crm=2, ic=5
       - Team C: pc=3, po=0, readiness=3, stock=3, crm=3, ic=3
       - Team D: pc=4, po=1, readiness=3, stock=3, crm=3, ic=3

    4. **Card IDs completeness:**
       - All 11 card IDs present: CS-01, CS-02, MP-01, MP-02, SP-01, SP-02, CP-01, PA-01, PA-02, PA-03, TR-01
       - Each card has non-empty name, cat, timing, req, and effect

    5. **National action IDs:**
       - All 4: NA-1, NA-2, NA-3, NA-4
       - Each has non-empty name, summary, and cost

    6. **Guide text fields non-empty:**
       - objective, redLines, pcThresholds, votingRule, eoMechanic, resourceLogic, facilitation are all non-empty strings with length > 50

    7. **Team metadata completeness:**
       - Each team has 2 personas (personas.length === 2)
       - Each team has non-empty uniqueAction
       - Each team has non-empty description

    Use `describe` blocks to group related tests. Use `it.each` for team validation to reduce repetition.

    Import from `@/data/edipConfig` using the path alias.

    AVOID:
    - Do NOT test implementation details — only test data shape and values
    - Do NOT use snapshot tests (brittle for large constants)
  </action>
  <verify>
    Run `pnpm vitest run src/data/edipConfig.test.ts` — ALL tests pass.
    Count the test assertions — should be 40+ individual checks across all test cases.
  </verify>
  <done>
    All EDIP config validation tests pass. Tests confirm correct counts (2 scenarios, 4 teams, 11 cards, 4 national actions), correct starting values for all teams, correct scenario round counts, and non-empty guide text fields.
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` — zero errors (EDIP_CONFIG satisfies GameConfig)
2. `pnpm vitest run src/data/edipConfig.test.ts` — all tests pass
3. `grep -c "id:" src/data/edipConfig.ts` — confirms all entities have IDs
4. Confirm EDIP_CONFIG.cards.length === 11 via test output
5. Confirm all 4 teams have correct starting resource values via test output
</verification>

<success_criteria>
- EDIP_CONFIG constant compiles with zero TypeScript errors against GameConfig interface
- 2 scenarios (S1: 4 rounds, S2: 5 rounds) with correct start states
- 4 teams with correct starting resource values (verified by tests)
- 11 cards with correct IDs and non-empty fields
- 4 national actions with correct IDs and non-empty fields
- All guide text fields (objective through facilitation) are non-empty
- All Vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
