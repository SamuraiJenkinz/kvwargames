---
phase: 12-crisis-state-prompt-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/promptBuilder.ts
  - src/lib/promptBuilder.test.ts
  - src/lib/stateUpdater.test.ts
  - src/lib/promptBudget.test.ts
autonomous: true

must_haves:
  truths:
    - "buildSystemPrompt() output contains an explicit MUST item in Finch's persona definition (Block 7) instructing her to advance crisisState per Block 9 threshold rules when crisisSeverity crosses 2 or 3"
    - "buildSystemPrompt() output contains a 'Crisis State Transition Rules' subsection in Block 9 that names the exact literal strings \"No Crisis\", \"Supply Crisis\", and \"Security-Related Supply Crisis\" with the severity thresholds 2 and 3"
    - "The new Block 9 subsection is located ABOVE the existing 'Clamp ranges' line and does not remove or alter the existing crisisState clamp-range line"
    - "An inline-snapshot test on the extracted Block 7 Finch section and extracted Block 9 crisis-transition subsection locks the rule text as a mechanical guardrail"
    - "A stateUpdater test confirms that a parsed Finch response carrying crisisState: \"Security-Related Supply Crisis\" passes through responseParser and applyStateUpdatePure and lands on gameState.crisisState with exact string equality"
    - "promptBudget test asserts report.withinLimit === true (not just boolean) so any future token regression fails CI"
    - "All 528+ existing tests remain green; new tests pass; npm run build succeeds"
  artifacts:
    - path: "src/lib/promptBuilder.ts"
      provides: "Block 7 Finch MUST list with new crisisState-advance item; Block 9 with new 'Crisis State Transition Rules' subsection above 'Clamp ranges'; top-of-file JSDoc pointing to 12-PROMPT-ENGINEERING-NOTES.md"
      contains: "Advance crisisState"
      contains_also:
        - "Crisis State Transition Rules"
        - "Security-Related Supply Crisis"
        - "12-PROMPT-ENGINEERING-NOTES"
    - path: "src/lib/promptBuilder.test.ts"
      provides: "Inline snapshot tests for Block 7 Finch section and Block 9 crisis-transition subsection; resilient block-isolation slicing"
      contains: "toMatchInlineSnapshot"
    - path: "src/lib/stateUpdater.test.ts"
      provides: "Integration test: responseParser + applyStateUpdatePure round-trip for crisisState: 'Security-Related Supply Crisis'"
      contains: "Security-Related Supply Crisis"
    - path: "src/lib/promptBudget.test.ts"
      provides: "New assertion `expect(report.withinLimit).toBe(true)` making budget regression a CI failure"
      contains: "expect(report.withinLimit).toBe(true)"
  key_links:
    - from: "Block 7 Finch MUST list"
      to: "Block 9 Crisis State Transition Rules subsection"
      via: "MUST text explicitly references 'Block 9' so the LLM can cross-reference within one prompt"
      pattern: "Advance crisisState per the threshold rules in Block 9"
    - from: "src/lib/promptBuilder.ts top-of-file JSDoc"
      to: ".planning/phases/12-crisis-state-prompt-engineering/12-PROMPT-ENGINEERING-NOTES.md"
      via: "Comment points future maintainers to the notes file before editing the transition rule"
      pattern: "12-PROMPT-ENGINEERING-NOTES"
    - from: "Inline snapshot test"
      to: "Standalone notes file"
      via: "Snapshot failure message should reference the notes path"
      pattern: "12-PROMPT-ENGINEERING-NOTES"
---

<objective>
Edit `src/lib/promptBuilder.ts` to encode the crisisState transition rule that was missing in v1.0 (see `.planning/milestones/v1.0-MILESTONE-AUDIT.md:31` and `.planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md:109-144`), and lock it in with automated tests.

**Purpose:** Close PROMPT-01 and PROMPT-02 by making the rule explicit in the Finch persona definition AND in the JSON Output Schema (belt-and-suspenders), and add a mechanical guardrail so the rule cannot silently disappear in future prompt edits.

**Output:**
- Two coordinated edits to `src/lib/promptBuilder.ts` (Block 7 + Block 9)
- Top-of-file JSDoc referencing the standalone notes file (file is created in Plan 12-02)
- Inline snapshot test on Block 7 Finch section
- Inline snapshot test on new Block 9 "Crisis State Transition Rules" subsection
- Integration test in `stateUpdater.test.ts` covering the parser → applier round-trip with the exact string literal
- Promoted `withinLimit === true` assertion in `promptBudget.test.ts`
</objective>

<execution_context>
@C:\Users\taylo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\taylo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-crisis-state-prompt-engineering/12-CONTEXT.md
@.planning/phases/12-crisis-state-prompt-engineering/12-RESEARCH.md

# Source under edit
@src/lib/promptBuilder.ts
@src/types/game.ts

# Test patterns to mirror
@src/lib/promptBuilder.test.ts
@src/lib/stateUpdater.test.ts
@src/lib/promptBudget.test.ts
@src/lib/responseParser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two coordinated edits to promptBuilder.ts (Block 7 Finch MUST + Block 9 transition subsection + top-of-file JSDoc)</name>
  <files>src/lib/promptBuilder.ts</files>
  <action>
Make three surgical edits to `src/lib/promptBuilder.ts` in a single commit:

**Edit A — Block 7 Finch MUST list (line 27–31 region)**

In `PERSONA_PROMPT_DEFS.finch.must`, append exactly one new array element after the existing `'Flag escalation thresholds (crisisSeverity movements).'` line. The new element text must be EXACTLY:

```
'Advance crisisState per the threshold rules in Block 9 when crisisSeverity crosses 2 or 3.',
```

Do NOT reword the other three `must` items. Do NOT alter Kent's or Chen's persona definitions. The Block 7 cross-reference to "Block 9" is load-bearing — the LLM uses it to locate the rule inside the same prompt.

**Edit B — Block 9 "Crisis State Transition Rules" subsection (inside `buildBlock9()`, around lines 212–213)**

Inside `buildBlock9()`, insert the following array elements IMMEDIATELY BEFORE the existing `'Clamp ranges (produce in-range values):'` line. Preserve the existing `''` blank line after `'stateUpdate is a DELTA ...'` so the new subsection is visually separated. The inserted elements are:

```
'Crisis State Transition Rules (Finch MUST emit these in stateUpdate):',
'- When crisisSeverity reaches 2 AND crisisState is "No Crisis":',
'  set crisisState to "Supply Crisis"',
'- When crisisSeverity reaches 3 AND crisisState is not "Security-Related Supply Crisis":',
'  set crisisState to "Security-Related Supply Crisis"',
'These transitions are emitted in the same turn the threshold is crossed. Kent and Chen do NOT emit crisisState transitions.',
'',
```

**Exact string literals matter.** `stateUpdater.ts:120–122` passes `crisisState` through with no enum validation (per 12-RESEARCH.md Q6). The three canonical values are (from `src/types/game.ts:3–6`):
- `"No Crisis"` (capital N, capital C)
- `"Supply Crisis"` (capital S, capital C)
- `"Security-Related Supply Crisis"` (capital S, capital R, capital S, capital C, hyphen between "Security" and "Related")

Do NOT alter the existing `- crisisState: one of "No Crisis" | "Supply Crisis" | "Security-Related Supply Crisis"` line in the clamp section — it stays. The new subsection documents the transition trigger; the clamp line documents allowed values. Both must coexist (RESEARCH.md Risk 3).

**Edit C — Top-of-file JSDoc**

Add a short JSDoc block at the very top of `src/lib/promptBuilder.ts`, BEFORE the existing `import type { ... }` on line 1. The JSDoc text must be short (3–6 lines) and must include the literal string `12-PROMPT-ENGINEERING-NOTES` so the snapshot test and future grep can locate the reference. Example acceptable wording:

```
/**
 * System prompt builder for the three-persona LLM call.
 *
 * LOAD-BEARING: The Finch crisisState transition rule (Block 7 MUST + Block 9
 * "Crisis State Transition Rules" subsection) is empirically required to make
 * Scenario 2 severity escalation produce a crisisState update. Before editing
 * those sections, read:
 *   .planning/phases/12-crisis-state-prompt-engineering/12-PROMPT-ENGINEERING-NOTES.md
 */
```

(Exact wording at Claude's discretion; the string `12-PROMPT-ENGINEERING-NOTES` and the word `Finch` must both appear.)

**Do not change anything else.** No reformatting of untouched blocks, no whitespace cleanup elsewhere, no reordering of array elements.
  </action>
  <verify>
- `npx vitest run src/lib/promptBuilder.test.ts --reporter=verbose` — all existing Block 7 and Block 9 tests still pass (they assert heading presence and substrings that are unchanged).
- `grep -n "Advance crisisState per the threshold rules in Block 9" src/lib/promptBuilder.ts` — exactly one match inside `PERSONA_PROMPT_DEFS.finch.must`.
- `grep -n "Crisis State Transition Rules" src/lib/promptBuilder.ts` — exactly one match inside `buildBlock9()`.
- `grep -n "Security-Related Supply Crisis" src/lib/promptBuilder.ts` — at least two matches: one in the new transition subsection, one in the existing clamp line.
- `grep -n "12-PROMPT-ENGINEERING-NOTES" src/lib/promptBuilder.ts` — exactly one match in the top-of-file JSDoc.
- `npm run build` succeeds (TypeScript still compiles).
  </verify>
  <done>
Block 7 Finch `must` array has 4 elements (was 3), the fourth being the crisisState-advance instruction that references Block 9. Block 9 contains a "Crisis State Transition Rules" subsection with all three literal crisisState strings and the thresholds 2 and 3, positioned above the existing clamp ranges line. Top-of-file JSDoc points to `12-PROMPT-ENGINEERING-NOTES.md`. Kent and Chen persona definitions are byte-identical to their pre-edit state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tier A automated tests — inline snapshots on Block 7 Finch + Block 9 transition subsection; parser/applier round-trip; promoted budget assertion</name>
  <files>
src/lib/promptBuilder.test.ts
src/lib/stateUpdater.test.ts
src/lib/promptBudget.test.ts
  </files>
  <action>
Add three sets of tests. Mirror existing patterns in each file (per 12-RESEARCH.md Q2). Do not add any new test files — keep each addition inside the file whose domain matches.

**Test set 1 — `src/lib/promptBuilder.test.ts` (inline snapshots)**

Add a new `describe('crisisState transition rule (PROMPT-01/PROMPT-02)', () => { ... })` block at the end of the file. Inside it, add two tests:

*Test 1a: Block 7 Finch section — inline snapshot*

Use the existing `makeMockState` factory and call `buildSystemPrompt(mockConfig, mockState)`. Extract the Finch subsection of Block 7 by slicing between the `### Finch` heading and the next `###` heading (or the `## 8.` heading, whichever comes first — use the tighter boundary). Assert with `toMatchInlineSnapshot` on the extracted slice. The snapshot should include the four MUST items (including the new one), the MUST NOT items, and the Voice line. Example shape (exact values will be filled in by Vitest on first run):

```typescript
const prompt = buildSystemPrompt(mockConfig, mockState)
const finchStart = prompt.indexOf('### Finch')
const chenStart = prompt.indexOf('### Chen', finchStart)
const finchSection = prompt.slice(finchStart, chenStart).trimEnd()
expect(finchSection).toMatchInlineSnapshot(/* Vitest fills on first run */)
```

On first run, let Vitest populate the inline snapshot via `npx vitest run src/lib/promptBuilder.test.ts -u`, then commit the populated snapshot.

*Test 1b: Block 9 "Crisis State Transition Rules" subsection — inline snapshot*

Extract the new subsection only, NOT the whole Block 9, to keep the snapshot resilient to unrelated schema edits. Slice between `'Crisis State Transition Rules'` and `'Clamp ranges'`. Assert with `toMatchInlineSnapshot`. Example:

```typescript
const prompt = buildSystemPrompt(mockConfig, mockState)
const ruleStart = prompt.indexOf('Crisis State Transition Rules')
const clampStart = prompt.indexOf('Clamp ranges', ruleStart)
expect(ruleStart).toBeGreaterThan(0)
expect(clampStart).toBeGreaterThan(ruleStart)
const ruleSection = prompt.slice(ruleStart, clampStart).trimEnd()
expect(ruleSection).toMatchInlineSnapshot(/* Vitest fills on first run */)
```

Both inline snapshots serve as the mechanical guardrail. A test-level comment should state: "If this snapshot fails, read `.planning/phases/12-crisis-state-prompt-engineering/12-PROMPT-ENGINEERING-NOTES.md` before updating it." This echoes the JSDoc in `promptBuilder.ts` (Task 1 Edit C) and turns snapshot failures into an explicit rule review, not a rubber-stamp update.

**Test set 2 — `src/lib/stateUpdater.test.ts` (parser → applier round-trip)**

Mirror the `makeState()` factory pattern already in the file. Add a new `it('parses and applies crisisState: "Security-Related Supply Crisis" end-to-end (PROMPT-01)', ...)` test inside the most appropriate existing describe block (or add a new `describe('crisisState pass-through (PROMPT-01)', ...)` group).

The test must:
1. Build a raw JSON string that a live Finch response would produce, e.g. `{"responses":[{"speaker":"finch","message":"...","stateUpdate":{"crisisSeverity":4,"crisisState":"Security-Related Supply Crisis"},"flag":null}]}`.
2. Call `parseLLMResponse(jsonString)` (from `responseParser.ts`).
3. Assert `result.ok === true`.
4. Pluck the Finch `stateUpdate` off the parsed result.
5. Call `applyStateUpdatePure(initialState, finchStateUpdate)`.
6. Assert `nextState.crisisState === 'Security-Related Supply Crisis'` (exact string equality).
7. Assert `nextState.crisisSeverity === 4` (confirms clamp passes the escalated severity through).

Also add a sibling test for the severity=2 transition: same structure, with `crisisSeverity:2, crisisState:"Supply Crisis"` and assert `nextState.crisisState === 'Supply Crisis'`.

Import `parseLLMResponse` from `./responseParser` and `applyStateUpdatePure` from `./stateUpdater`. If the existing file already imports one of them, reuse the import.

**Test set 3 — `src/lib/promptBudget.test.ts` (promoted assertion)**

Find the existing test that verifies `withinLimit` is a boolean matching the arithmetic (per 12-RESEARCH.md Risk 4). Directly BELOW it, add a new `it('PROMPT-budget: withinLimit is true with current prompt (no regression)', ...)` that calls `reportPromptBudget()` and asserts:

```typescript
expect(report.withinLimit).toBe(true)
expect(report.totalCeilingEstimate).toBeLessThanOrEqual(7500)
```

This converts a budget blowout from a silent `console.info: withinLimit: false` into a CI failure. Current empirical headroom per 12-RESEARCH.md is ~656 tokens after the ~120-token rule addition — this assertion will pass on first run with the Task 1 edits applied.

**Do NOT add any other tests.** The prose of Finch's response varies by LLM and is out of scope for Tier A (that's Tier B's job in Plan 12-02). Out-of-enum `crisisState` strings are out of phase scope (12-CONTEXT.md deferred).
  </action>
  <verify>
- `npx vitest run src/lib/promptBuilder.test.ts src/lib/stateUpdater.test.ts src/lib/promptBudget.test.ts --reporter=verbose` — all tests pass, including the two new inline snapshots, two new round-trip tests, and one new budget assertion.
- First-time inline snapshots should be generated with `npx vitest run src/lib/promptBuilder.test.ts -u` then re-run without `-u` to confirm they are stable.
- `npx vitest run` (full suite) — 528+ existing tests plus the new tests all pass.
- `grep -n "toMatchInlineSnapshot" src/lib/promptBuilder.test.ts` — exactly two matches (Finch section + transition subsection).
- `grep -n "Security-Related Supply Crisis" src/lib/stateUpdater.test.ts` — at least one match inside the new round-trip test.
- `grep -n "expect(report.withinLimit).toBe(true)" src/lib/promptBudget.test.ts` — exactly one match.
  </verify>
  <done>
Tier A is complete: two inline snapshots lock the prompt rule text, two parser/applier round-trip tests confirm the exact string literals pass through the stack, and the budget-regression assertion turns a future token blowout into a CI failure. Full test suite is green.
  </done>
</task>

</tasks>

<verification>
Phase-level gate for this plan:

1. `npm run build` succeeds.
2. `npx vitest run` — full suite green, including the new tests.
3. `grep -n "Advance crisisState per the threshold rules in Block 9" src/lib/promptBuilder.ts` — 1 match.
4. `grep -n "Crisis State Transition Rules" src/lib/promptBuilder.ts` — 1 match.
5. `grep -n "12-PROMPT-ENGINEERING-NOTES" src/lib/promptBuilder.ts` — 1 match.
6. `npx vitest run src/lib/promptBudget.test.ts --reporter=verbose 2>&1 | grep "withinLimit"` — shows `withinLimit: true` and `totalCeilingEstimate ≤ 7500`.
7. Open `src/lib/promptBuilder.ts` and visually confirm the new Block 9 subsection sits ABOVE the "Clamp ranges" line; confirm the existing clamp line for `crisisState` is unchanged.

If any step fails, stop and diagnose before proceeding to Plan 12-02. In particular, if step 6 reports `withinLimit: false`, the prose of the new Block 9 subsection must be tightened; the phase budget is hard.
</verification>

<success_criteria>
**Closes the following Phase 12 success criteria (full closure in 12-02 for criterion #2):**
- Criterion #1 (explicit transition rule in system prompt): YES — Block 7 Finch MUST + Block 9 subsection.
- Criterion #3 (rule documented so future edits preserve it): PARTIAL — JSDoc in source + inline snapshots as mechanical guardrail. Standalone notes file is written in Plan 12-02.
- Criterion #2 (Scenario 2 severity=4 replay produces transition): NOT YET — requires live-LLM replay in Plan 12-02.

**Measurable completion:**
- 528+ existing tests pass; 5 new tests pass (2 snapshots, 2 round-trip, 1 budget).
- `promptBuilder.ts` diff is confined to: (a) top-of-file JSDoc, (b) one new element in `PERSONA_PROMPT_DEFS.finch.must`, (c) 7 new lines in `buildBlock9()` array.
- Total token budget stays under 7500 with `withinLimit === true`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-crisis-state-prompt-engineering/12-01-SUMMARY.md` following the standard summary template. Include:
- `tech-stack.added`: (none — all existing)
- `patterns.established`: "Inline snapshot tests for load-bearing prompt sections (first repo use of `toMatchInlineSnapshot`)"
- `key-files`: updated `promptBuilder.ts`, `promptBuilder.test.ts`, `stateUpdater.test.ts`, `promptBudget.test.ts`
- `requires-next`: "Plan 12-02 writes the `12-PROMPT-ENGINEERING-NOTES.md` file that `promptBuilder.ts` JSDoc points to, and performs Tier B live-LLM replay."
- `open-items`: closed PROMPT-01 (rule encoded), closed PROMPT-02 (documented in source via JSDoc + tests); PROMPT-03 still open pending Tier B.
</output>
