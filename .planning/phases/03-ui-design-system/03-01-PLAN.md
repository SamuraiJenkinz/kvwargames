---
phase: 03-ui-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-ui-design-system/stitch-reference/
autonomous: true

must_haves:
  truths:
    - "A dark-themed layout reference directional exists as committed artifact"
    - "Token extraction notes capture any Stitch-suggested values for comparison with spec"
  artifacts:
    - path: ".planning/phases/03-ui-design-system/stitch-reference/"
      provides: "Stitch output screenshots or exported assets"
  key_links: []
---

<objective>
Use Google Stitch MCP tool to generate dark-themed game layout screen directionals from the spec. Capture and commit the output as design reference for future phases.

Purpose: Provide visual direction for the ops/command center aesthetic before building components. This is best-effort — all token hex values are already authoritative from the spec. Stitch output informs layout decisions in Phase 5, not token values.
Output: Stitch-generated layout reference images/exports committed to the phase directory.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-design-system/03-RESEARCH.md
@.planning/phases/03-ui-design-system/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate dark-themed layout directionals via Google Stitch</name>
  <files>.planning/phases/03-ui-design-system/stitch-reference/</files>
  <action>
Use the mcp__stitch__* MCP tools to generate dark-themed game layout screens. The design brief for Stitch should specify:

- **Aesthetic**: Military briefing room / situation awareness display. NOT consumer SaaS.
- **Background**: Near-black (#060810) with cool-toned dark panels
- **Layout**: Three-column game screen — left StatePanel (~210px), center ChatFeed (flexible), right ReferencePanel (~252px)
- **Header**: Game title, round indicator, scenario name
- **Colour palette**: Use persona colours (Kent blue #5B9BD5, Finch amber #DFA02A, Chen green #2BC48A) as accents against dark backgrounds
- **Typography**: Syne for display headings, DM Sans for body, IBM Plex Mono for data values and section headers
- **Components**: Flat/outlined with sharp corners (rounded-sm 2px). No glassmorphism, no gradients, no rounded cards.
- **Density**: Balanced — key info prominent, details on interaction

Generate at minimum:
1. Game screen (three-column layout with chat feed, state panel, reference panel)
2. Setup/home screen (two-path: load config vs generate from brief)

Save all Stitch outputs (screenshots, exports, URLs) to `.planning/phases/03-ui-design-system/stitch-reference/`.

If Stitch MCP tools are unavailable or fail, create a `stitch-reference/SKIPPED.md` file documenting the attempt and noting that all design tokens are already defined from the spec. This plan is best-effort and does not block any downstream work.
  </action>
  <verify>
Directory `.planning/phases/03-ui-design-system/stitch-reference/` exists and contains at least one file (either Stitch output or SKIPPED.md).
  </verify>
  <done>Stitch directional design reference is captured and committed, OR skip is documented. Either outcome is acceptable — the plan is best-effort.</done>
</task>

</tasks>

<verification>
- [ ] Stitch reference directory exists with content
- [ ] If Stitch succeeded: layout reference images show dark-themed ops aesthetic
- [ ] If Stitch failed: SKIPPED.md documents the attempt
</verification>

<success_criteria>
A design reference artifact exists in the phase directory. This does NOT block any subsequent plans — all token values are authoritative from the spec regardless of Stitch output.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-design-system/03-01-SUMMARY.md`
</output>
