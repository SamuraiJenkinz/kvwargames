---
phase: 03-ui-design-system
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/components/dev/TokenReference.tsx
autonomous: false

must_haves:
  truths:
    - "TokenReference page renders at 1280px without horizontal scroll"
    - "TokenReference page is usable at 768px — no overlap or cut-off content"
    - "All colour swatches, badges, and track bars are visible at both breakpoints"
  artifacts:
    - path: "src/components/dev/TokenReference.tsx"
      provides: "Responsive token reference page"
  key_links: []
---

<objective>
Validate the TokenReference page renders correctly at 1280px (desktop) and 768px (tablet) breakpoints. Fix any layout issues — wrapping, overflow, cut-off content.

Purpose: Ensure the design system foundation is responsive before building real components against it. The component reference page must be usable at both target breakpoints.
Output: Responsive TokenReference page verified at both breakpoints.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dev/TokenReference.tsx
@src/styles/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate and fix responsive layout</name>
  <files>src/components/dev/TokenReference.tsx</files>
  <action>
Read the TokenReference component and evaluate its responsive behaviour at 1280px and 768px:

1. **1280px check**: The max-w-4xl (896px) container fits comfortably. Verify no horizontal overflow from the card category row (7 chips) or resource dots row (6 dots). If the horizontal row of 7 category chips overflows at smaller container widths, switch to a `flex flex-wrap gap-3` layout.

2. **768px check**: At tablet width, ensure:
   - The horizontal swatch rows wrap properly (flex-wrap)
   - Persona tinted panels stack vertically if needed
   - Track bar containers don't overflow
   - Typography samples remain readable
   - Section spacing is appropriate (reduce py-12 to py-6 on sm:)

3. **Apply fixes** to TokenReference.tsx:
   - Use `flex flex-wrap gap-3` for any horizontal row that could overflow at 768px
   - Add responsive padding: `px-4 sm:px-8 py-6 sm:py-12`
   - Ensure grid layouts use `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` where applicable
   - Persona swatches: `flex flex-col sm:flex-row gap-4`

4. **Verify no hardcoded widths** that could cause overflow — prefer w-full, max-w-*, or flex layouts over fixed pixel widths.
  </action>
  <verify>
`npx tsc --noEmit` passes. `npx vite build --mode development` succeeds. Grep for any fixed pixel widths that could cause overflow: `grep -E "w-\[[0-9]+px\]" src/components/dev/TokenReference.tsx` should return 0 results (small inline swatches like w-12 h-12 are fine — those are Tailwind spacing, not fixed pixel widths).
  </verify>
  <done>TokenReference layout uses responsive Tailwind classes. No fixed widths that could cause overflow at 768px.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete design token system: Tailwind v4 @theme tokens, Google Fonts, custom scrollbars, and a visual TokenReference page — all rendered at desktop and tablet breakpoints.</what-built>
  <how-to-verify>
1. Run `npm run dev` in the project root
2. Open browser to http://localhost:5173 (or whatever port Vite assigns)
3. **Desktop check (1280px):**
   - Page should show dark background with all token sections
   - Three persona colour swatches (blue, amber, green) with tinted panels
   - Seven card category colour chips in a row
   - Three crisis state badges (green "NO CRISIS", amber "SUPPLY CRISIS", red "SECURITY CRISIS")
   - Two track bars (red severity, blue legitimacy) with sample fills
   - Typography samples in three fonts (Syne display, DM Sans body, IBM Plex Mono data)
   - No horizontal scroll at 1280px width
4. **Tablet check (768px):**
   - Resize browser to 768px width
   - All content should be visible without horizontal scroll
   - Rows should wrap naturally — no cut-off or overlapping elements
5. **Scrollbar check:** If any section has overflow content, scrollbar should be thin and subtle (3px, dark grey thumb)
6. **Font check:** Verify three distinct fonts are loading (Syne has distinctive thick strokes, IBM Plex Mono is monospaced, DM Sans is the clean body font)
  </how-to-verify>
  <resume-signal>Type "approved" if all tokens render correctly at both breakpoints, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- [ ] TokenReference page renders all token groups
- [ ] No horizontal scroll at 1280px
- [ ] Usable layout at 768px (no overlap, no cut-off)
- [ ] Custom scrollbars visible on overflow
- [ ] Three fonts load and render correctly
- [ ] Human verification confirms visual correctness
</verification>

<success_criteria>
The TokenReference page is visually confirmed correct at both 1280px and 768px breakpoints. All design tokens render through Tailwind utility classes. The design system is complete and ready for Phase 4+ component development.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-design-system/03-04-SUMMARY.md`
</output>
