---
phase: 03-ui-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/index.css
  - index.html
autonomous: true

must_haves:
  truths:
    - "All seven card category colours resolve as Tailwind utility classes (e.g. bg-category-crisis)"
    - "All three crisis badge colours resolve as Tailwind utility classes"
    - "Three Google Fonts (Syne, DM Sans, IBM Plex Mono) load and render correctly"
    - "Custom thin scrollbars appear on overflow containers"
    - "Body has dark background, primary text colour, and DM Sans font applied"
  artifacts:
    - path: "src/styles/index.css"
      provides: "Complete @theme token block and @layer base styles"
      contains: "--color-category-crisis"
    - path: "index.html"
      provides: "Google Fonts preconnect and stylesheet links"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "src/styles/index.css"
      to: "index.html"
      via: "Font families declared in @theme reference fonts loaded in index.html"
      pattern: "--font-display.*Syne"
---

<objective>
Complete the Tailwind v4 @theme token block with all missing design tokens and add Google Fonts loading + custom scrollbar styles.

Purpose: Establish the complete visual language so every subsequent component can use Tailwind utility classes (bg-category-crisis, text-persona-kent, font-display) without hardcoding any hex values.
Output: Fully populated index.css with all tokens, index.html with font links, global scrollbar styles.
</objective>

<execution_context>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-design-system/03-RESEARCH.md
@src/styles/index.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete @theme token block and @layer base styles in index.css</name>
  <files>src/styles/index.css</files>
  <action>
Replace the current index.css with the complete token definition. The current file has ~60% of tokens (backgrounds, borders, text, personas, resources, fonts). Add ALL missing tokens:

**Add to @theme block (missing tokens):**
```
/* Card Categories */
--color-category-crisis:     #FF6B6B;
--color-category-monitoring: #74B9FF;
--color-category-prio-soft:  #FDCB6E;
--color-category-prio-hard:  #E17055;
--color-category-demand:     #55EFC4;
--color-category-production: #81ECEC;
--color-category-transfers:  #A29BFE;

/* Crisis States */
--color-crisis-none:     #2BC48A;
--color-crisis-supply:   #FDCB6E;
--color-crisis-security: #FF6B6B;

/* Track Bars */
--color-track-severity:   #FF6B6B;
--color-track-legitimacy: #5B9BD5;

/* Border Radius */
--radius-sm: 2px;
--radius-md: 4px;
--radius-lg: 6px;

/* Animations */
--animate-blink: blink 1.4s ease-in-out infinite;

@keyframes blink {
  0%, 100% { opacity: 0.25; transform: scale(0.8); }
  50%       { opacity: 1;   transform: scale(1);   }
}
```

**Add @layer base block (new):**
```css
@layer base {
  ::-webkit-scrollbar       { width: 3px; height: 3px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--color-border-muted); border-radius: 2px; }
  *                          { scrollbar-width: thin; scrollbar-color: var(--color-border-muted) transparent; }

  body {
    background-color: var(--color-bg-base);
    color: var(--color-text-primary);
    font-family: var(--font-body);
    -webkit-font-smoothing: antialiased;
  }
}
```

Keep ALL existing tokens unchanged. The complete file should match the target state from 03-RESEARCH.md exactly.

**Anti-patterns to avoid:**
- Do NOT create a tailwind.config.ts file — Tailwind v4 is CSS-first
- Do NOT use :root CSS variables — use @theme which Tailwind v4 reads natively
- Do NOT hardcode hex values anywhere except inside the @theme block
  </action>
  <verify>
Run `npx tailwindcss --help` to confirm Tailwind is installed. Then verify the CSS parses by running `npx vite build --mode development 2>&1 | head -20` — build should not fail on CSS syntax errors. Grep for all token names: `grep -c "color-category\|color-crisis\|color-track\|radius-\|animate-blink\|font-display\|font-body\|font-mono" src/styles/index.css` should return at least 20 matches.
  </verify>
  <done>index.css contains all token groups: backgrounds (4), borders (4), text (3), personas (3), resources (6), card categories (7), crisis states (3), track bars (2), fonts (3), radii (3), animation (1), plus @layer base with scrollbar and body styles.</done>
</task>

<task type="auto">
  <name>Task 2: Add Google Fonts preconnect and stylesheet links to index.html</name>
  <files>index.html</files>
  <action>
Add Google Fonts loading to index.html `<head>` section. Use preconnect links for performance, then the stylesheet link.

Add these lines inside `<head>`, before the `<title>` tag:

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&family=Syne:wght@600;700;800&display=swap">
```

**Font weights loaded:**
- Syne: 600, 700, 800 (display headings, scenario titles)
- DM Sans: 400, 500, 600 (body text, buttons, labels)
- IBM Plex Mono: 400, 500 (data values, section headers, monospace content)

**Why preconnect in HTML not CSS @import:** CSS @import blocks rendering; HTML preconnect + link allows parallel loading. This is the research-recommended approach.
  </action>
  <verify>
Grep index.html for font links: `grep "fonts.googleapis" index.html` should show the stylesheet link. Run `npx vite build --mode development 2>&1 | tail -5` to confirm build still succeeds.
  </verify>
  <done>index.html loads all three Google Fonts families with correct weights via preconnect + stylesheet link.</done>
</task>

</tasks>

<verification>
- [ ] `grep -c "color-category" src/styles/index.css` returns 7 (all card categories)
- [ ] `grep -c "color-crisis" src/styles/index.css` returns 3 (all crisis states)
- [ ] `grep "fonts.googleapis" index.html` shows font stylesheet link
- [ ] `npx vite build --mode development` completes without CSS errors
- [ ] `grep "scrollbar-width" src/styles/index.css` returns 1 (scrollbar applied)
</verification>

<success_criteria>
All design tokens are defined in the Tailwind v4 @theme block. Components in subsequent phases can use utility classes like `bg-category-crisis`, `text-persona-kent`, `font-display`, `rounded-sm` without any hex values. Google Fonts load in the browser. Custom scrollbars render on overflow containers. Body has dark background with correct font.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-design-system/03-02-SUMMARY.md`
</output>
