---
phase: 11-polish-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/mocks/seedMockState.ts
  - src/components/setup/LoadConfigPanel.test.tsx
  - src/lib/debriefExporter.test.ts
  - .planning/REQUIREMENTS.md
autonomous: false

must_haves:
  truths:
    - "Navigating directly to /game with gameState=null redirects to /setup (no blank screen, no DEV auto-seed)"
    - "Browser console shows no 'setState called during render' warning during normal facilitation use (fresh /game redirect, Setup->Launch->Game, full round of interaction)"
    - "Round 1 facilitator input in the downloaded debrief export starts with its actual first character (full 'Round 1 is now live...' text, not 'ound 1...')"
    - "Existing 528-test suite remains green after changes"
  artifacts:
    - path: "src/App.tsx"
      provides: "GuardedGameScreen with silent <Navigate to='/setup' replace /> for null state"
      contains: "return <Navigate to=\"/setup\" replace />"
      must_not_contain: "seedMockState"
    - path: "src/mocks/seedMockState.ts"
      provides: "DELETED — file must not exist"
      state: "removed"
    - path: "src/lib/debriefExporter.test.ts"
      provides: "DEBRIEF-01 regression test asserting R1 facilitator text preserves leading character"
      contains: "DEBRIEF-01"
  key_links:
    - from: "src/App.tsx GuardedGameScreen"
      to: "react-router-dom Navigate"
      via: "return <Navigate to='/setup' replace />"
      pattern: "Navigate.*to=.\"?/setup.\"?.*replace"
    - from: "DEBRIEF-01 regression test"
      to: "generateDebriefMarkdown"
      via: "assertion on exported markdown string"
      pattern: "generateDebriefMarkdown"
---

<objective>
Fix three known v1.0 defects in a single atomic plan:
1. **ROUTE-01**: `/game` with null gameState must redirect to `/setup` unconditionally (no DEV auto-seed).
2. **ROUTE-02**: React "setState called during render" warning at `gameStore.ts:304` must be gone during normal facilitation use (fixed by construction when DEV auto-seed is removed).
3. **DEBRIEF-01**: Round 1 facilitator input in the downloaded debrief export must start with its actual first character — no "ound 1..." truncation.

Purpose: Close the last three operational defects from the v1.0 live run so the next live exercise starts from a clean pipeline. These are small, independent bug fixes — not features, not refactors.

Output: A cleaner App.tsx, a deleted mocks/seedMockState.ts, a regression test for DEBRIEF-01 (plus a targeted fix if the test can locate the bug), and a clean browser console during facilitation.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-polish-bug-fixes/11-CONTEXT.md
@.planning/phases/11-polish-bug-fixes/11-RESEARCH.md

# Bug 1/2 primary sites
@src/App.tsx
@src/mocks/seedMockState.ts

# Bug 2 claimed warning site (read-only — no edit expected)
@src/lib/gameStore.ts

# Bug 3 candidate surfaces (all ruled out by static analysis per RESEARCH.md, but load for traceability when writing the failing test)
@src/lib/debriefExporter.ts
@src/lib/debriefExporter.test.ts
@src/lib/responseParser.ts
@src/lib/contextWindow.ts

# Existing routing regression coverage (may need post-fix cleanup)
@src/components/setup/LoadConfigPanel.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove DEV auto-seed — fixes ROUTE-01 and ROUTE-02 by construction</name>
  <files>
    src/App.tsx
    src/mocks/seedMockState.ts
    src/components/setup/LoadConfigPanel.test.tsx
  </files>
  <action>
Delete the DEV auto-seed code path in GuardedGameScreen. This single change fixes both ROUTE-01 (no auto-seeded DEV state on null-state /game hits) and ROUTE-02 (the "setState called during render" warning, which is caused by `seedMockState()` calling store mutations synchronously inside the render body — NOT by `gameStore.ts:304` itself).

**Exact edits in `src/App.tsx`:**
1. Delete line 3: `import { seedMockState } from '@/mocks/seedMockState'`
2. Delete lines 22–28 inclusive — the entire `if (import.meta.env.DEV) { seedMockState(); return null }` block.

After edits, `GuardedGameScreen` must collapse to exactly this shape (per RESEARCH.md):
```typescript
function GuardedGameScreen() {
  const gameState = useGameStore((s) => s.gameState)
  if (gameState === null) {
    return <Navigate to="/setup" replace />
  }
  return <GameScreen />
}
```

**Delete `src/mocks/seedMockState.ts` entirely.** RESEARCH.md confirmed via grep that `App.tsx:3` is the file's sole real consumer — `LoadConfigPanel.test.tsx:417` is a comment-only reference. Use `git rm src/mocks/seedMockState.ts` or equivalent. **Do NOT delete `src/mocks/mockGameState.ts`** — it is imported by other component tests (ChatFeed, GameScreen) and must be kept.

**Clean up the now-dead `vi.stubEnv('DEV', false)` call in `src/components/setup/LoadConfigPanel.test.tsx` around line 422.** After the DEV branch is gone, the stub is a no-op. Either remove the single `vi.stubEnv('DEV', false)` line or leave a one-line comment explaining the historical reason — your call. Do NOT remove or rewrite the surrounding `AppRoutes` redirect test; it still covers the /game → /setup contract and must keep passing.

**Do NOT:**
- Add a `setTimeout` / `queueMicrotask` / `useEffect` workaround around store setters in `gameStore.ts` — that would mask the render-purity violation instead of fixing it. The store setters are correct; the caller was wrong.
- Gate the DEV branch behind a new flag ("in case we need it back"). CONTEXT.md is explicit: remove, don't hide. Dead code leaves the bug reintroducible.
- Add a toast / flash message to the redirect. Silent `<Navigate replace />` is the decision — direct /game hits are almost always accidents (refresh, bookmark, copy-paste).
- Edit any other `import.meta.env.DEV` guards in the codebase (they exist at `gameStore.ts:258,444,452,673` for dev-only console output, outside any render path, and are fine).
  </action>
  <verify>
1. `npx vitest run` — full suite must stay green (528 tests expected to pass).
2. `grep -r "seedMockState" src/` returns no import/usage hits (comment-only hit in `LoadConfigPanel.test.tsx:417` is acceptable if still present, but the `vi.stubEnv('DEV', false)` line should be removed or commented).
3. `ls src/mocks/seedMockState.ts` returns "No such file or directory".
4. `npm run build` succeeds (no TypeScript error from the removed import).
5. `grep -n "Navigate" src/App.tsx` shows exactly one `<Navigate to="/setup" replace />` line inside `GuardedGameScreen`, and no `seedMockState` reference anywhere in the file.
  </verify>
  <done>
- `src/App.tsx` is 6 lines shorter in `GuardedGameScreen`, no `seedMockState` import, `GuardedGameScreen` matches the canonical shape above.
- `src/mocks/seedMockState.ts` deleted (tracked in git deletion).
- `LoadConfigPanel.test.tsx`'s existing `AppRoutes` redirect test still passes; dead `vi.stubEnv('DEV', false)` line cleaned up or annotated.
- Full `npx vitest run` green.
- `npm run build` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: DEBRIEF-01 — write failing test, diagnose, branch on outcome</name>
  <files>
    src/lib/debriefExporter.test.ts
    src/lib/debriefExporter.ts          # edit only if test fails and points here
    src/lib/gameStore.ts                # edit only if test fails and points here
    src/lib/responseParser.ts           # edit only if test fails and points here
    .planning/REQUIREMENTS.md           # edit only in branch B below
  </files>
  <action>
RESEARCH.md explicitly states: "DEBRIEF-01 root cause NOT located via static analysis." Static analysis ruled out `renderMessage`, `sendFacilitatorMessage`, `contextWindow.ts:55 .slice(1)`, `responseParser.ts:106 .slice(1)`, and `generateDebriefMarkdown` assembly. This task is **test-first diagnosis** with a pre-agreed branch on the test outcome — do NOT invent a fix without evidence.

**Step A — Write the failing test FIRST.**

Append to `src/lib/debriefExporter.test.ts` a new test inside the existing `describe('generateDebriefMarkdown', ...)` block (or a sibling block — your call, co-location is fine). Use the existing fixture helpers already in that file. The test must assert that a Round 1 facilitator message whose text is `"Round 1 is now live. Kent, set the scene."` survives the full export pipeline with its leading "R" intact.

Canonical assertions (phrasing may be refined; these two assertions are the load-bearing ones):
```typescript
it('DEBRIEF-01 regression: R1 facilitator message preserves leading character', () => {
  // Arrange a DebriefSnapshot with a Round 1 facilitator message containing "Round 1 is now live..."
  // then a debrief_divider, then a persona message (minimal shape to satisfy the renderer).
  // Use the existing baseSnapshot / fixture helpers in the test file.
  const md = generateDebriefMarkdown(snapshot)
  expect(md).not.toContain('**Facilitator:** ound 1 is now live')
  expect(md).toContain('**Facilitator:** Round 1 is now live')
})
```

Use the exact `ChatMessage` shape from the candidate test in RESEARCH.md (lines 164–199) as the starting point, adapting to whatever fixture helpers already exist in `debriefExporter.test.ts`. Run `npx vitest run src/lib/debriefExporter.test.ts` immediately after writing the test.

**Step B — Branch on the test outcome.**

**Branch A: test FAILS.** The bug is in the pure function pipeline. Trace from the assertion backwards — inspect the actual `md` output, compare to expected, follow the string through `generateDebriefMarkdown` → `renderRound` → `renderMessage`. Likely culprits (in priority order per CONTEXT.md): (1) round-start auto-injected facilitator messages in `gameStore.ts` around `sendFacilitatorMessage` / round-advance logic, (2) a misapplied `.slice(1)` somewhere the static pass missed, (3) `renderRound` header/formatting around `debriefExporter.ts:108–130`. Make the **minimum surgical edit** to fix it — a one-line fix at the off-by-one site is the expected shape. Re-run the test — it must now pass. **Also audit Rounds 2+** in the test fixture (add a Round 2 message to the same test or a sibling test) to confirm the fix is not R1-specific; CONTEXT.md requires this audit.

**Branch B: test PASSES.** The bug is NOT reproducible in the pure-function pipeline. This confirms RESEARCH.md's Open Question 1: the truncation is a browser/OS download artifact (keyboard hotkey swallow, Blob boundary byte, or serialization edge case during live run). Do NOT invent a fix. Instead:
  1. **Keep the test in the file** — it is a regression guard that pins the invariant even though the bug lives elsewhere.
  2. Edit `.planning/REQUIREMENTS.md`: mark DEBRIEF-01 as `[x]` with a suffix note: `(closed 2026-04-15 — not reproducible in pure function pipeline; assumed browser/OS download artifact from v1.0 live run, regression test in debriefExporter.test.ts pins the invariant)`. Update the traceability table entry from "Pending" to `Complete (browser artifact)`.
  3. Do NOT edit any production source file in this branch. That is the finding.

**Scope guard (ship-fast rule):** If diagnosis in Branch A uncovers **6 or more affected call sites** or a systemic message-construction pattern defect (not a localized off-by-one), STOP editing and surface the finding — per CONTEXT.md "Deferred" section, that warrants a separate refactor phase, not Phase 11 expansion. Return the evidence and let the user decide.

**Do NOT:**
- Add E2E / Playwright tests for the debrief export. CONTEXT.md says "one regression test at the boundary where the bug was found" — keep it a unit test on the pure function.
- Add multiple assertions exploring related concerns (message ordering, timestamps, divider rendering). One test, two assertions (the negative and positive), done.
- Modify existing debrief tests. Append only.
  </action>
  <verify>
1. `npx vitest run src/lib/debriefExporter.test.ts` — new DEBRIEF-01 test PASSES (either because the source was fixed in Branch A, or because the bug was never in this pipeline in Branch B).
2. `npx vitest run` — full suite green (528+1 = 529 tests, or 528+N if the Rounds 2+ audit added more cases in Branch A).
3. In Branch A only: `git diff` on the fixed source file shows a surgical one- to three-line edit, not a rewrite.
4. In Branch B only: `.planning/REQUIREMENTS.md` has DEBRIEF-01 marked `[x]` with the browser-artifact note and traceability table updated; no production source file in the working tree diff.
  </verify>
  <done>
- New regression test exists in `src/lib/debriefExporter.test.ts` and passes.
- Full test suite is green.
- Either: (Branch A) a targeted fix is in place at the located off-by-one site, verified against Rounds 2+ in the same test or a sibling test, OR (Branch B) REQUIREMENTS.md is updated with the browser-artifact finding and the test is retained as a guard.
- The which-branch decision is recorded explicitly in the summary for traceability.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke — three facilitation paths, clean console</name>
  <what-built>
- DEV auto-seed removed from `src/App.tsx`; `src/mocks/seedMockState.ts` deleted.
- GuardedGameScreen now unconditionally redirects null-state `/game` hits to `/setup`.
- DEBRIEF-01 regression test added (and either a source fix landed in Branch A, or the browser-artifact finding recorded in Branch B).
- Full automated suite (`npx vitest run`) is green and `npm run build` succeeds.

What automation cannot verify: the absence of the "setState called during render" warning during interactive facilitation. This warning surfaces only in the browser DevTools console during real navigation + store interaction, not in the Vitest jsdom environment. CONTEXT.md explicitly requires empirical console validation across three paths before marking this phase complete.
  </what-built>
  <how-to-verify>
Run `npm run dev` (default http://localhost:5173) and open browser DevTools → Console tab. Clear the console. Walk all three paths below and confirm the console stays free of any React warning — in particular the "Cannot update a component while rendering a different component" / "setState called during render" message that was observed at `gameStore.ts:304` in v1.0.

1. **Null-state redirect path (ROUTE-01 empirical check):**
   - Navigate directly to `http://localhost:5173/game` with no prior setup (hard refresh or new incognito window).
   - Expected: URL changes to `/setup` immediately, no blank screen, no flash of game UI, no console warning.

2. **Normal Setup → Launch → Game flow:**
   - Start at `/setup`. Load or generate a config, proceed through to the Launch step, click Launch Scenario.
   - Expected: Arrive at `/game` with the chat feed and state dashboard visible. Console clean.

3. **Full round of facilitator interaction (ROUTE-02 empirical check):**
   - From the Game screen, type a facilitator message, send it, wait for persona responses, type another message or advance the round.
   - Expected: Messages flow, state dashboard updates, console stays completely clean of any "setState called during render" warning across multiple send/response cycles.

Optional but recommended: export a debrief from a short session and open the downloaded markdown file. Confirm the first facilitator message text (e.g., "Round 1 is now live...") starts with its real first character. This is an empirical second check on DEBRIEF-01 complementing the unit test.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 11 complete, or describe any warning / behaviour that didn't match.</resume-signal>
</task>

</tasks>

<verification>
**Automated (must pass before checkpoint):**
- `npx vitest run` — full suite green (expect 529+ tests after the DEBRIEF regression test lands).
- `npm run build` — clean TypeScript compile + Vite build.
- `grep -r "seedMockState" src/` — no import / call-site hits.
- `ls src/mocks/seedMockState.ts` — file does not exist.

**Manual (checkpoint Task 3):**
- Null-state redirect works silently.
- Console clean across all three facilitation paths.
- (Optional) Debrief export preserves leading character in R1 facilitator text.

**Traceability:**
- ROUTE-01 satisfied by: `src/App.tsx` `GuardedGameScreen` shape + Task 3 path 1 verification.
- ROUTE-02 satisfied by: DEV auto-seed removal (Task 1) + Task 3 paths 2–3 console verification.
- DEBRIEF-01 satisfied by: Task 2 regression test (both branches) + either source fix (Branch A) or REQUIREMENTS.md annotation (Branch B).
</verification>

<success_criteria>
All three ROADMAP.md Phase 11 success criteria hold:

1. Navigating directly to `/game` with no loaded game state redirects to `/setup` — no blank screen, no auto-seeded DEV state. *(Verified automatically via existing AppRoutes test + manually via Task 3 path 1.)*

2. The browser console is free of the "setState called during render" warning at `gameStore.ts:304` during normal facilitation use. *(Verified manually via Task 3 paths 2–3; fixed by construction via DEV auto-seed removal.)*

3. Round 1 facilitator input text in the downloaded debrief export starts with its actual first character — no "ound 1 is now live..." truncation. *(Verified automatically via the DEBRIEF-01 regression test in either branch outcome; optionally verified manually via a real export in Task 3.)*

Additionally:
- Existing 528-test suite remains green; the added regression test brings the count to 529+.
- `npm run build` succeeds.
- No production source file is edited beyond what the three bugs strictly require (no drive-by refactors).
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-bug-fixes/11-01-SUMMARY.md` following the summary template. Include:

- Which DEBRIEF-01 branch was taken (A = source fixed, B = browser-artifact finding).
- Final test count delta from 528.
- Exact files modified / deleted.
- Any findings surfaced during DEBRIEF-01 diagnosis that the scope guard declined to expand into (for future Deferred tracking).
- Confirmation of Task 3 resume-signal ("approved" or issue description) for audit trail.
</output>
