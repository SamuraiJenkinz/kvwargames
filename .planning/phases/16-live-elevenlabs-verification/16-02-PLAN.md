---
phase: 16-live-elevenlabs-verification
plan: 02
plan_id: "16-02"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - .planning/milestones/v1.2-MILESTONE-AUDIT.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  validated_via_audit:
    - PODGEN-01
    - PODGEN-02
    - PODGEN-03
    - PODGEN-04
    - PODGEN-05
    - PODGEN-06
    - PODGEN-07
    - PODGEN-08
    - PODPLAY-01
    - PODPLAY-02
    - PODPLAY-03
    - PODPLAY-04
    - PODPLAY-05
    - PODUX-01
    - PODUX-02
    - PODUX-03
    - PODRES-01
    - PODRES-02
    - PODRES-03
    - PODDEP-01
    - PODDEP-02
must_haves:
  truths:
    - "`.planning/milestones/v1.2-MILESTONE-AUDIT.md` exists and is structurally identical to `v1.1-MILESTONE-AUDIT.md` — YAML frontmatter shape matches; body sections (Milestone Goal Achievement, Requirements Coverage, Phase Verification Summary, Cross-Phase Integration, E2E Flows, Tech Debt Register, Anti-Patterns Scan, Test Suite Evidence, Human Verification Evidence, Recommendation) all present in the same order"
    - "Audit frontmatter status is exactly one of `shipped` or `tech_debt` (both are ship-acceptable per v1.1 precedent); status `blocked` would halt the plan"
    - "Audit scores line shows `requirements: 21/21`, `phases: 4/4`, `integration: 4/4`, `flows: 3/3`"
    - "Requirements Coverage table has all 21 v1.2 requirements (PODGEN-01..08, PODPLAY-01..05, PODUX-01..03, PODRES-01..03, PODDEP-01..02) each with `SATISFIED` status and an evidence link"
    - "Phase Verification Summary table has rows for Phases 13, 14, 15, 16 — each with must-have counts sourced from the respective VERIFICATION.md frontmatter"
    - "Audit cites all four per-phase VERIFICATION.md files + 16-LIVE-VERIFICATION.md via relative-path links"
    - "REQUIREMENTS.md footer is updated with a new dated note that cites 16-LIVE-VERIFICATION.md; no new columns introduced; existing row statuses unchanged"
    - "PROJECT.md Key Decisions table has a new v1.2-milestone-shipped entry matching the v1.1 precedent shape (v1.1 entry was added at the same point by v1.1's audit)"
    - "ROADMAP.md Phase 16 row shows 2/2 Complete; v1.2 milestone is marked shipped at the top"
    - "STATE.md current-focus updated to reflect v1.2 shipped / post-v1.2 state"
  artifacts:
    - path: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      provides: "v1.2 milestone audit report"
      contains: "milestone: v1.2"
      min_lines: 90
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table updated with v1.2 shipped entry"
      contains: "v1.2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "footer note citing 16-LIVE-VERIFICATION.md"
      contains: "16-LIVE-VERIFICATION.md"
    - path: ".planning/ROADMAP.md"
      provides: "v1.2 milestone marked shipped, Phase 16 row 2/2 complete"
      contains: "v1.2 Debrief Podcast"
  key_links:
    - from: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      to: ".planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md"
      via: "Requirements Coverage table evidence column + §Human Verification Evidence"
      pattern: "16-LIVE-VERIFICATION\\.md"
    - from: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      to: ".planning/phases/13-firewall-spike-mockable-backend-foundation/13-VERIFICATION.md"
      via: "Phase Verification Summary row 13"
      pattern: "13-VERIFICATION\\.md"
    - from: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      to: ".planning/phases/14-podcast-endpoint-player/14-VERIFICATION.md"
      via: "Phase Verification Summary row 14"
      pattern: "14-VERIFICATION\\.md"
    - from: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      to: ".planning/phases/15-tts-health-graceful-degradation/15-VERIFICATION-AUDIT.md"
      via: "Phase Verification Summary row 15 + Human Verification Evidence row"
      pattern: "15-VERIFICATION"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md"
      via: "footer *Last updated* note"
      pattern: "16-LIVE-VERIFICATION\\.md"
---

<objective>
Author the v1.2 milestone audit, update PROJECT.md Key Decisions, update REQUIREMENTS.md footer, and flip ROADMAP.md v1.2 to shipped — all from the committed evidence produced by 16-01 and the three prior per-phase VERIFICATION.md files. This is pure desk research + document authoring. No code, no execution, no network calls.

Purpose: Close Roadmap SC4 (milestone audit confirming 21/21 requirements, 4/4 phases, graceful-degradation empirically verified, no blocking tech debt, matching v1.1-MILESTONE-AUDIT.md shape). Without this plan, v1.2 cannot formally ship even though 16-01 produced the evidence — the audit is the milestone's ship certificate.

Output: committed `.planning/milestones/v1.2-MILESTONE-AUDIT.md` + updated PROJECT.md + updated REQUIREMENTS.md + updated ROADMAP.md + updated STATE.md, all cross-referencing each other and 16-LIVE-VERIFICATION.md as the live-validation authority.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# v1.1 audit — THE template this plan mirrors
@.planning/milestones/v1.1-MILESTONE-AUDIT.md

# Phase-level VERIFICATION files the audit cites
@.planning/phases/13-firewall-spike-mockable-backend-foundation/13-VERIFICATION.md
@.planning/phases/14-podcast-endpoint-player/14-VERIFICATION.md
@.planning/phases/15-tts-health-graceful-degradation/15-VERIFICATION-AUDIT.md
@.planning/phases/15-tts-health-graceful-degradation/15-VERIFICATION.md

# 16-01 evidence — the last input to this audit
@.planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md
@.planning/phases/16-live-elevenlabs-verification/16-01-SUMMARY.md

# Phase 16 context/research — for cross-referencing decisions
@.planning/phases/16-live-elevenlabs-verification/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v1.2-MILESTONE-AUDIT.md by mirroring v1.1 audit shape exactly</name>
  <files>
    .planning/milestones/v1.2-MILESTONE-AUDIT.md
  </files>
  <action>
    Mirror `.planning/milestones/v1.1-MILESTONE-AUDIT.md` section-by-section. Do NOT invent new sections or new frontmatter keys.

    **Step A — Pre-audit data gathering.** Read in parallel:
    - `.planning/phases/13-firewall-spike-mockable-backend-foundation/13-VERIFICATION.md` — Phase 13 verdict + must-have count + any tech-debt notes
    - `.planning/phases/14-podcast-endpoint-player/14-VERIFICATION.md` — Phase 14 verdict + must-have count + any tech-debt notes
    - `.planning/phases/15-tts-health-graceful-degradation/15-VERIFICATION-AUDIT.md` — Phase 15 automated audit result
    - `.planning/phases/15-tts-health-graceful-degradation/15-VERIFICATION.md` — Phase 15 empirical evidence (garbage-key run, banner screenshots, markdown SHA-256)
    - `.planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md` — Phase 16 Tier-B live-run evidence (from 16-01)
    - `.planning/phases/16-live-elevenlabs-verification/16-01-SUMMARY.md` — Phase 16 Plan 1 summary including SC1/SC2/SC3 verdicts and any Tier-A/B dispositions
    - `.planning/PROJECT.md` current Key Decisions table — for shape reference
    - `.planning/v1.1-INTEGRATION-CHECK.md` if it exists (v1.1 audit cited it; if there's a v1.2 equivalent, cite it; if not, synthesize the integration check inline)

    Extract:
    - Current backend pytest count (post 16-01 Task 1; expect ~142+)
    - Current frontend vitest count (post 16-01 Task 1; expect ~627+)
    - Any Tier-B deferrals from 16-LIVE-VERIFICATION.md §5 (voice quality observations that went to v1.3 VOICE-01 backlog)
    - Any Tier-A fixes applied during 16-01 Task 3 (preprocessor dict updates + golden-corpus additions)
    - The WMP-vs-VLC duration quirk status: does 16-LIVE-VERIFICATION.md §4 mention any variance between CBR-computed duration and VLC-reported duration? If yes and it persists with real ElevenLabs output, Tech Debt Register row; if no, omit.
    - v1.1 inherited tech debt: check if `TD-v1.1-01` (ROADMAP.md:198 Phase 11 "0/1 Not started") was closed in the interim. Grep `.planning/ROADMAP.md` — the current ROADMAP has Phase 11 as `1/1 Complete 2026-04-15` (line 112), so TD-v1.1-01 was closed during the v1.2 kickoff edits. Note this in the Tech Debt Register as "v1.1 inherited debt: closed (no action required)".

    **Step B — Author the file.** Write `.planning/milestones/v1.2-MILESTONE-AUDIT.md` with the exact structure below. All the `[FILL IN]` tokens are populated from Step A data.

    ```markdown
    ---
    milestone: v1.2
    audited: [ISO-8601 timestamp — use current date 2026-04-19 or actual run date with Z suffix]
    status: [shipped | tech_debt]  # shipped if zero tech-debt items; tech_debt if any items; NEVER blocked (if blocked, halt per escalation affordance below)
    scores:
      requirements: 21/21
      phases: 4/4
      integration: 4/4
      flows: 3/3
    gaps:
      requirements: []
      integration: []
      flows: []
    tech_debt:
      # Populate from Step A. Each item:
      # - phase: <short-id>
      #   severity: low | medium | high  # high = should NOT ship; escalate
      #   items:
      #     - "text of the debt item"
      # Candidates (list if present, omit if absent):
      # - Any Tier-B voice-quality observation from 16-LIVE-VERIFICATION.md deferred to v1.3 VOICE-01
      # - WMP duration quirk if it persisted with real ElevenLabs output (Low)
      # - Any ROADMAP/doc-drift surfaced during audit read (Low)
      # If zero items, use: tech_debt: []  (and set status: shipped)
    ---

    # Milestone v1.2 — Debrief Podcast — Audit Report

    **Audited:** [YYYY-MM-DD — match the `audited` frontmatter date portion]
    **Status:** [shipped | tech_debt] ([one-sentence justification — mirror v1.1 "no blockers; one low-severity doc drift" phrasing])
    **Scope:** Phases 13–16

    ## Milestone Goal Achievement

    **Goal:** [Copy verbatim from ROADMAP.md lines 11-13 — the v1.2 Milestone Goal paragraph.]

    **Verdict:** ACHIEVED. [One paragraph: the four phases delivered the three-voice podcast MVP on fake then empirically verified against real ElevenLabs at the Scenario-2 fixture; graceful degradation was empirically proven in Phase 15; the Tier-B evidence bundle is committed at `16-LIVE-VERIFICATION.md`.]

    ## Requirements Coverage (21/21 SATISFIED)

    | ID | Description | Phase | Status |
    |----|-------------|-------|--------|
    | PODGEN-01 | Facilitator can trigger podcast generation from Generate Podcast button | 14 | SATISFIED |
    | PODGEN-02 | Three voice segments in fixed Kent → Finch → Chen order | 14 | SATISFIED (live evidence: `16-LIVE-VERIFICATION.md`) |
    | PODGEN-03 | Each persona reads existing isDebrief messages verbatim (no new LLM call) | 14 | SATISFIED |
    | PODGEN-04 | ~700ms silence between segments, no leading/trailing pad | 14 | SATISFIED (live evidence: `16-LIVE-VERIFICATION.md` §3 offsets) |
    | PODGEN-05 | Wargame vocabulary pronounced correctly + numbers normalised | 13 | SATISFIED (live acronym listen-through: `16-LIVE-VERIFICATION.md` §5 deviation table) |
    | PODGEN-06 | Word-count soft ceiling triggers confirmation dialog | 14 | SATISFIED |
    | PODGEN-07 | In-memory cache serves unchanged debrief | 14 | SATISFIED |
    | PODGEN-08 | Re-generate button invalidates cache with confirmation | 14 | SATISFIED |
    | PODPLAY-01 | Inline audio controls element pre-loaded paused (no auto-play) | 14 | SATISFIED (live evidence: `16-LIVE-VERIFICATION.md` `evidence/player-screenshot.png`) |
    | PODPLAY-02 | Download MP3 filename `debrief-{kebab}-{YYYY-MM-DD-HHmm}.mp3` | 14 | SATISFIED |
    | PODPLAY-03 | Expandable transcript panel using existing markdown renderer | 14 | SATISFIED |
    | PODPLAY-04 | Three Skip buttons seek audio to per-persona offsets | 14 | SATISFIED (live evidence: `16-LIVE-VERIFICATION.md` §4 offsets JSON) |
    | PODPLAY-05 | Now-playing label updates at segment boundaries | 14 | SATISFIED |
    | PODUX-01 | Per-persona status line during generation | 14 | SATISFIED |
    | PODUX-02 | Overall progress bar with percentage | 14 | SATISFIED |
    | PODUX-03 | Cancel button aborts in-flight generation | 14 | SATISFIED |
    | PODRES-01 | ElevenLabs failure never breaks markdown debrief path | 15 | SATISFIED (empirical evidence: `15-VERIFICATION.md` garbage-key run) |
    | PODRES-02 | TTS health badge informational, does NOT gate Launch | 15 | SATISFIED |
    | PODRES-03 | `GET /api/health/tts` — 8-code taxonomy, 15s SLA, always HTTP 200 | 15 | SATISFIED |
    | PODDEP-01 | ElevenLabs reachable from corporate firewall | 13 | SATISFIED (operational precedent + HTTP 200 `/v1/voices` preflight; streaming-payload evidence: `16-LIVE-VERIFICATION.md` §3) |
    | PODDEP-02 | FakeTTSProvider selected via `TTS_PROVIDER` env var | 13 | SATISFIED |

    ## Phase Verification Summary

    | Phase | Must-Haves | Status | Tech Debt |
    |-------|-----------|--------|-----------|
    | 13. Firewall Spike + Mockable Backend Foundation | [count from 13-VERIFICATION.md, typically N/N] | passed | [from 13-VERIFICATION.md or None] |
    | 14. Podcast Endpoint + Player (End-to-End on Fake) | [count from 14-VERIFICATION.md] | passed | [from 14-VERIFICATION.md; note WMP duration quirk here if mentioned] |
    | 15. TTS Health + Graceful Degradation | [count from 15-VERIFICATION-AUDIT.md, typically 4/4] | passed | [from 15-VERIFICATION-AUDIT.md or None] |
    | 16. Live ElevenLabs Verification + Milestone Audit | [count — Tier-B evidence does not use the same must-have scoring as auto-verified phases; cite as "SC1/SC2/SC3 PASS per `16-LIVE-VERIFICATION.md` §5"] | passed | [any Tier-B deferrals here; or None if none] |

    ## Cross-Phase Integration (4/4 CONNECTED)

    [Author four paragraphs, mirroring v1.1-MILESTONE-AUDIT.md lines 53-61. For each integration point, cite the specific files + anchors. Suggested:]

    1. **Phase 13 → 14:** `FakeTTSProvider` (`backend/app/services/tts/fake_provider.py`) feeds `audio_generator.py` which powers the POST `/api/debrief/podcast` endpoint registered at `main.py:120`. Raw-bytes stitcher consumes FakeTTSProvider output with the same `mp3_44100_128` CBR formula real ElevenLabs uses — 16-LIVE-VERIFICATION.md §4 proves the assumption holds end-to-end.
    2. **Phase 14 → 15:** `usePodcastStore` error-setter is called by both the transport layer (podcastClient.ts) and the TTS health badge (TtsHealthBadge.tsx) via the shared error-code taxonomy from `src/types/tts-health.ts`. Phase 15-03 empirically verified that a PODRES-01 failure surfaces through this shared channel while leaving the markdown debrief download intact.
    3. **Phase 15 → 16:** `TTS_PROVIDER=elevenlabs` path exercised by Phase 15's empirical garbage-key run returned the `upstream_error` code correctly; Phase 16's real-key run produced the `ok` path correctly through the same code — the error-code dispatch table is now empirically verified on both branches.
    4. **Phase 16 bundled fix:** `ActionToolbar.tsx` sentinel voice IDs (`__fake_kent__` etc.) replaced with `GET /api/config/tts-voices` endpoint + `src/lib/ttsVoicesClient.ts`. Browser path now posts real voice IDs; the fake path returns the preserved sentinels unchanged. Integration tested in 16-01 Task 1.

    ## E2E Flows (3/3 COMPLETE)

    [Mirror v1.1 lines 63-67. Three flows per 16-RESEARCH.md Section 3 notes:]

    - **A. Fake-provider dev/CI path:** `TTS_PROVIDER=fake` → `POST /api/debrief/podcast` → FakeTTSProvider fixtures → stitched MP3 from fake bytes → browser player loads paused. Verified: Phase 14 VERIFICATION + backend pytest + frontend vitest.
    - **B. Live-provider path:** `TTS_PROVIDER=elevenlabs` + real API key + v0.10 voice IDs → `POST /api/debrief/podcast` → ElevenLabs TTS → real stitched MP3 → VLC listen-through PASS. Verified: `16-LIVE-VERIFICATION.md` Tier-B evidence bundle.
    - **C. Graceful-degradation path:** ElevenLabs down / garbage key → podcast banner error with structured code → markdown debrief download still completes with valid .md. Verified: `15-VERIFICATION.md` empirical garbage-key run (downloaded .md SHA-256 b00eda8…).

    ## Tech Debt Register

    [Populate table from Step A extraction. Columns: ID | Severity | Item | Action. ID naming: `TD-v1.2-NN` per v1.1 precedent. If empty table, write: "No tech debt. No anti-patterns detected across any phase verification."]

    | ID | Severity | Item | Action |
    |----|----------|------|--------|
    | [e.g., TD-v1.2-01] | [Low/Med/High] | [e.g., "Tier-B voice-quality deviation for term X — deferred to v1.3 VOICE-01"] | [e.g., "Input captured into v1.3 VOICE-01 backlog; no action this milestone."] |

    [Note: `status: shipped` requires this section to be empty or "No tech debt." Any filled row here means `status: tech_debt`. v1.1 shipped as `tech_debt` with one Low item (doc drift) and that was explicitly acceptable.]

    ## Anti-Patterns Scan

    Zero TODO/FIXME/stub/placeholder markers detected in Phase 13/14/15/16 load-bearing code per individual VERIFICATION.md reports. [Confirm by running the grep yourself during audit authoring: `grep -rnE "TODO|FIXME|XXX|HACK|stub|placeholder" backend/app/services/tts/ backend/app/services/audio_generator.py backend/app/services/text_preprocessor.py backend/app/routers/debrief.py backend/app/routers/health_tts.py backend/app/routers/config_tts.py src/components/game/Podcast/ src/lib/podcastStore.ts src/lib/podcastClient.ts src/lib/ttsVoicesClient.ts` — if any match, list them here with severity. If zero, state so.]

    ## Test Suite Evidence

    - **Frontend:** [N]/[N] green (Vitest, [M] test files) at audit time — source of truth is the 16-01 Task 1 verify step count.
    - **Backend:** [N]/[N] green (pytest) at audit time — source of truth is the 16-01 Task 1 verify step count.
    - **Production build:** `tsc -b && vite build` succeeds (last verified in 16-01 Task 1).

    ## Human Verification Evidence

    - **Phase 14 end-to-end on fake:** VLC playback of stitched fake MP3 (documented in `14-VERIFICATION.md`).
    - **Phase 15 empirical graceful degradation:** screenshots of [upstream_error] banner + SHA-256 b00eda8… of downloaded markdown debrief (`15-VERIFICATION.md`).
    - **Phase 16 live ElevenLabs listen-through:** VLC listen-through of real-key stitched MP3 against Scenario-2 fixture; 8-row acronym deviation table; player-state screenshot (`16-LIVE-VERIFICATION.md` §5 + `evidence/player-screenshot.png`).

    ## Recommendation

    [One paragraph mirroring v1.1-MILESTONE-AUDIT.md line 93-94 shape. Template: "Milestone is substantively complete. [If tech_debt: The N items in the Tech Debt Register are [all Low severity / safely deferred to v1.3 VOICE-01 / etc.]. Close them during `/gsd:complete-milestone v1.2` or the v1.3 kickoff.] [If shipped: No tech debt, no open items, ready for v1.3 planning.]"]

    ---

    *Audit by gsd-audit-milestone orchestrator (manual pass per v1.1 precedent)*
    *Evidence sources: `.planning/phases/1{3,4,5,6}-*/*-VERIFICATION*.md`, `.planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md`*
    ```

    **Escalation affordance:** If during pre-audit data gathering in Step A any of the following is observed, HALT and surface the blocker instead of authoring the audit:
    - Any per-phase VERIFICATION.md reports `status: failed` or contains uncleared gap items
    - 16-LIVE-VERIFICATION.md §5 verdict was FAIL on SC1, or any Tier-C row exists in the deviation table
    - Any of the 21 requirements in REQUIREMENTS.md shows a status other than `Complete`
    - Backend pytest or frontend vitest is red at audit time

    In all four cases, do NOT ship with `status: blocked` — `blocked` is explicitly not an acceptable ship state per 16-CONTEXT.md. Halt the plan and surface the issue for escalation (likely a Phase 17 insert).
  </action>
  <verify>
    - `.planning/milestones/v1.2-MILESTONE-AUDIT.md` exists.
    - YAML frontmatter parses with keys exactly: `milestone`, `audited`, `status`, `scores`, `gaps`, `tech_debt`. No extra keys, no missing keys.
    - Frontmatter `status` is either `shipped` or `tech_debt` (NOT `blocked`).
    - Frontmatter `scores` shows `requirements: 21/21`, `phases: 4/4`, `integration: 4/4`, `flows: 3/3`.
    - `gaps.requirements`, `gaps.integration`, `gaps.flows` are all `[]`.
    - Body has sections matching regex `^## (Milestone Goal Achievement|Requirements Coverage|Phase Verification Summary|Cross-Phase Integration|E2E Flows|Tech Debt Register|Anti-Patterns Scan|Test Suite Evidence|Human Verification Evidence|Recommendation)$` — exactly 10 section headings, in that order.
    - Requirements Coverage table has exactly 21 data rows (grep `^\| POD` and count).
    - Phase Verification Summary table has exactly 4 data rows (one per Phase 13/14/15/16).
    - Cross-reference links resolve: `grep -oE "\[.*\]\([^)]*VERIFICATION[^)]*\.md[^)]*\)" .planning/milestones/v1.2-MILESTONE-AUDIT.md` returns links to 13/14/15/16 verification files.
    - Citation to `16-LIVE-VERIFICATION.md` appears at least 3 times (Requirements Coverage evidence col + Phase 16 row + Human Verification section).
    - Citation to `15-VERIFICATION.md` appears at least twice (Requirements PODRES-01 evidence + Human Verification).
    - `wc -l .planning/milestones/v1.2-MILESTONE-AUDIT.md` ≥ 90.
  </verify>
  <done>
    `v1.2-MILESTONE-AUDIT.md` is written, structurally identical to the v1.1 audit (YAML frontmatter shape + 10 body sections in the same order), contains all 21 requirements marked SATISFIED with evidence links, Phase Verification Summary covers all 4 phases, E2E Flows covers fake + live + graceful-degradation paths, and cross-references resolve to real files. Status is `shipped` if Tech Debt Register is empty, `tech_debt` if any rows present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md footer + PROJECT.md Key Decisions + ROADMAP.md milestone status + STATE.md current focus</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
    Four targeted document edits. Each one is a small, surgical change; no refactors.

    **Edit 1 — `.planning/REQUIREMENTS.md` footer.**

    The current footer (lines 118-120):
    ```
    ---
    *Requirements defined: 2026-04-17*
    *Last updated: 2026-04-18 — Phase 13 complete (PODDEP-01, PODDEP-02, PODGEN-05 marked Complete); 3/21 requirements delivered*
    ```

    Append (do NOT replace — add a new line AFTER the existing `*Last updated: 2026-04-18...*` line):
    ```
    *Last updated: [YYYY-MM-DD] — v1.2 milestone audited [shipped|tech_debt]; all 21 requirements empirically validated against real ElevenLabs endpoint (evidence: `.planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md`)*
    ```

    Use the actual audit date from Task 1 frontmatter. Status word must match Task 1 frontmatter `status`. Do NOT introduce a new "Validated" column on the Traceability table — 16-CONTEXT.md explicitly rejects this (deferred-ideas list). The link from the audit to the live-verification record IS the validation evidence. All existing row statuses remain `Complete`.

    **Edit 2 — `.planning/PROJECT.md` Key Decisions table.**

    Read the current Key Decisions table in `.planning/PROJECT.md`. At the end of the table (following the v1.1 milestone-shipped entry shape as precedent), add a new row for the v1.2 milestone ship. Match the v1.1 precedent row shape exactly — columns typically are `# | Date | Decision | Rationale | Outcome` or similar (use whatever the current PROJECT.md uses; do NOT change column structure).

    Content (adjust column fit to PROJECT.md's actual schema):
    - **Date:** [audit date from Task 1 frontmatter]
    - **Decision:** v1.2 Debrief Podcast milestone shipped — 21/21 requirements validated against real ElevenLabs
    - **Rationale:** Tier-B evidence bundle (`16-LIVE-VERIFICATION.md`) demonstrates end-to-end three-voice MP3 generation from Scenario-2 fixture using real v0.10 voice IDs; graceful degradation empirically verified in Phase 15; firewall reachability empirically verified in Phase 13
    - **Outcome:** [Good if status: shipped; Good-with-deferral if status: tech_debt with all items low-severity; each Tier-B voice observation captured as v1.3 VOICE-01 input]

    If PROJECT.md has a different shape for milestone entries (e.g., a separate "Shipped Milestones" section), add the entry to that section in the shape used for v1.1. The rule is: match the v1.1 precedent structure exactly; do not invent new fields.

    **Edit 3 — `.planning/ROADMAP.md` milestone status + Phase 16 row.**

    Two changes in ROADMAP.md:

    3a. **Top "Milestones" section** (line ~7 currently shows `🚧 **v1.2 Debrief Podcast** — Phases 13–16 (in progress)`):
    Change to: `✅ **v1.2 Debrief Podcast** — Phases 13–16 (shipped [YYYY-MM-DD]) — audit: [\`milestones/v1.2-MILESTONE-AUDIT.md\`](milestones/v1.2-MILESTONE-AUDIT.md)`

    3b. **Phase 16 entry in the Phases list** (line ~26): the current `- [ ] **Phase 16:...` becomes `- [x] **Phase 16:... ✅ [YYYY-MM-DD]` mirroring Phase 13/14/15 entries (lines 23-25 for shape).

    3c. **Phase 16 plan list** (lines ~91-93):
    - Flip `- [ ] 16-01:...` → `- [x] 16-01: Live ElevenLabs Tier-B replay against Scenario-2 debrief fixture + commit raw MP3 + per-segment offsets + acronym listen-through notes — completed [YYYY-MM-DD]`
    - Flip `- [ ] 16-02:...` → `- [x] 16-02: v1.2 milestone audit + PROJECT.md Key Decisions update + REQUIREMENTS.md status flip to Validated — completed [YYYY-MM-DD]`

    3d. **Progress table at bottom** (line ~117): `| 16. Live ElevenLabs Verification + Milestone Audit | v1.2 | 0/2 | Not started | - |` becomes `| 16. Live ElevenLabs Verification + Milestone Audit | v1.2 | 2/2 | Complete | [YYYY-MM-DD] |`.

    3e. **Optional — new "Current Milestone" reset.** If the `## Current Milestone` section (currently points at v1.2) should advance, leave it as-is for now and let the `/gsd:complete-milestone v1.2` orchestrator or the next milestone kickoff handle the reset. This plan's scope is shipping v1.2, not opening v1.3.

    **Edit 4 — `.planning/STATE.md` current-focus update.**

    Edit the "## Current Position" block (lines 10-14 currently). Update:
    - `Phase: 15 of 16` → `Phase: 16 of 16`
    - `Plan: 3 of 3 in current phase` → `Plan: 2 of 2 in current phase`
    - `Status: Phase 15 complete...` → `Status: v1.2 Debrief Podcast shipped [YYYY-MM-DD]; Phase 16 Tier-B evidence bundle + milestone audit complete; all 21 requirements validated`
    - `Last activity:` line → update with [YYYY-MM-DD] and one-line summary of this plan's completion

    Update the progress bar: `Progress: [███████████████████░] 57/58 plans complete` → `Progress: [████████████████████] 58/58 plans complete — v1.0 (39) + v1.1 (7) + v1.2 (12) shipped`. Adjust the count to match actual plan numbers (v1.2 was projected at 12 with 16-02 being the 12th).

    Update "**Shipped Milestones:**" table: flip the v1.2 row from `🚧 In progress (Phases 13 + 14 + 15 complete 2026-04-19)` to `✅ Tagged` with shipped date.

    Do NOT add new Decisions to STATE.md Accumulated Context — the v1.2 decisions are now captured in PROJECT.md. STATE.md's v1.2-plan-specific decisions lists can remain for session continuity, but the milestone-level decision is PROJECT.md's responsibility.

    **Verification after each edit:**
    - `python -m json.tool` does NOT apply (these are markdown files); instead confirm via `grep` the new content is present.
    - After all four edits, re-run an integration check: `.planning/milestones/v1.2-MILESTONE-AUDIT.md` references `REQUIREMENTS.md`; REQUIREMENTS.md footer references `16-LIVE-VERIFICATION.md`; ROADMAP.md references the audit file. No broken mutual links.
  </action>
  <verify>
    **Edit 1 — REQUIREMENTS.md:**
    - `tail -5 .planning/REQUIREMENTS.md` shows the new `*Last updated:...v1.2 milestone audited...*` line as the LAST line of the file.
    - `grep -c "16-LIVE-VERIFICATION.md" .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -c "| Status |" .planning/REQUIREMENTS.md` returns exactly the same count as before the edit — no new column added.
    - All 21 requirement rows still show status `Complete` — zero rows changed.

    **Edit 2 — PROJECT.md:**
    - `grep -c "v1.2" .planning/PROJECT.md` returns ≥ previous-count + 1 (new row added).
    - The v1.2 shipped entry is in the same section/table as the v1.1 shipped entry (`grep -B 2 -A 2 "v1.2" .planning/PROJECT.md | head -20` shows consistent context with v1.1 entry).

    **Edit 3 — ROADMAP.md:**
    - `grep "v1.2 Debrief Podcast" .planning/ROADMAP.md` — the first match (top Milestones section) contains `✅` and a shipped date and link to `milestones/v1.2-MILESTONE-AUDIT.md`.
    - Phase 16 row in the Phases section starts with `- [x]` not `- [ ]`.
    - Both Plan 16-01 and Plan 16-02 checkboxes are `[x]`.
    - Progress table row 16 shows `2/2 | Complete`.

    **Edit 4 — STATE.md:**
    - `grep "Phase: 16 of 16" .planning/STATE.md` returns 1 match.
    - `grep "Plan: 2 of 2" .planning/STATE.md` returns 1 match.
    - `grep "v1.2 Debrief Podcast.*Tagged\|v1.2.*shipped" .planning/STATE.md` returns ≥ 1 match.
    - Progress bar shows 58/58 (or whatever the actual v1.2 total plan count is) plans complete.

    **Integration check (all edits):**
    - `grep -l "16-LIVE-VERIFICATION.md" .planning/milestones/v1.2-MILESTONE-AUDIT.md .planning/REQUIREMENTS.md` returns both files.
    - `grep -l "v1.2-MILESTONE-AUDIT.md" .planning/ROADMAP.md` returns ROADMAP.md.
    - No markdown link-syntax errors in any edited file (basic check: `grep -oE "\[[^]]*\]\([^)]*\)" <file> | wc -l` returns a sensible count and visual scan of links looks correct).
  </verify>
  <done>
    REQUIREMENTS.md has a new footer line dated with the audit date, citing 16-LIVE-VERIFICATION.md; no new column added; all existing row statuses preserved. PROJECT.md Key Decisions has a new v1.2-shipped entry matching v1.1 precedent shape. ROADMAP.md reflects v1.2 shipped at the top, Phase 16 marked complete in the Phases list + Progress table, both 16-01 and 16-02 plan checkboxes flipped. STATE.md "Current Position" shows Phase 16/16 Plan 2/2 shipped; progress bar updated; Shipped Milestones table shows v1.2 as Tagged. All cross-file links resolve; integration-check greps confirm mutual references.
  </done>
</task>

</tasks>

<verification>

**Plan-level verification:**

1. **Roadmap SC4 (milestone audit structure match):**
   - `.planning/milestones/v1.2-MILESTONE-AUDIT.md` YAML frontmatter keys are an exact match to v1.1-MILESTONE-AUDIT.md keys: `diff <(head -n 20 .planning/milestones/v1.1-MILESTONE-AUDIT.md | grep -oE "^[a-z_]+:" | sort -u) <(head -n 20 .planning/milestones/v1.2-MILESTONE-AUDIT.md | grep -oE "^[a-z_]+:" | sort -u)` returns empty.
   - Body section headings (level-2) are identical sets: `diff <(grep -oE "^## .*$" .planning/milestones/v1.1-MILESTONE-AUDIT.md) <(grep -oE "^## .*$" .planning/milestones/v1.2-MILESTONE-AUDIT.md)` returns at most minor count differences, and all v1.1 section names appear in v1.2.
   - Status ∈ {`shipped`, `tech_debt`} — NEVER `blocked`.

2. **21/21 requirements coverage:**
   - Audit Requirements Coverage table has exactly 21 data rows, each with `SATISFIED` status.
   - Every requirement ID in REQUIREMENTS.md:83-105 appears in the audit table.

3. **4/4 phases covered:**
   - Audit Phase Verification Summary has exactly 4 data rows for Phases 13, 14, 15, 16.
   - Each row cites a VERIFICATION.md file that exists on disk.

4. **E2E flows (3/3):**
   - Audit §E2E Flows has exactly 3 flow entries (A fake-provider, B live-provider, C graceful-degradation) each with a verification source citation.

5. **Cross-reference integrity:**
   - Every relative-path link in the audit resolves to a file on disk: for each `[text](path)` extracted from the audit, confirm `test -f <resolved-path>` returns 0.
   - `REQUIREMENTS.md` footer cites `16-LIVE-VERIFICATION.md` via an existing relative path.
   - `ROADMAP.md` top-level v1.2 entry cites `milestones/v1.2-MILESTONE-AUDIT.md` via an existing relative path.

6. **No regression to prior milestones:**
   - `grep "v1.0" .planning/milestones/v1.1-MILESTONE-AUDIT.md` content unchanged (sanity check the edit touched only v1.2 scope).
   - `grep "v1.1" .planning/PROJECT.md` entries unchanged count.

7. **Status chain consistent:**
   - If audit frontmatter `status: shipped`, then PROJECT.md v1.2 row Outcome is `Good`, STATE.md Shipped Milestones row for v1.2 is `✅ Tagged`, and ROADMAP.md top Milestones entry shows `✅`.
   - If audit frontmatter `status: tech_debt`, same chain with PROJECT.md Outcome = `Good (with N low-severity deferrals)` and ROADMAP.md still shows `✅` (tech_debt is ship-acceptable per v1.1 precedent).

8. **Autonomous plan sanity:**
   - No checkpoints in this plan — every task is `type="auto"`. Operator confirmation is NOT required at any point in this plan's execution; halt-on-blocker affordance handles the only escape hatch.

</verification>

<success_criteria>

This plan is complete when:

- [ ] `.planning/milestones/v1.2-MILESTONE-AUDIT.md` exists with frontmatter exactly mirroring v1.1-MILESTONE-AUDIT.md (milestone/audited/status/scores/gaps/tech_debt keys), scores showing 21/21, 4/4, 4/4, 3/3, and 10 body sections in the same order as v1.1.
- [ ] Audit frontmatter `status` is `shipped` or `tech_debt`; never `blocked`. If any blocker surfaced during authoring, the plan halted at the escalation affordance in Task 1 instead of setting `blocked`.
- [ ] Requirements Coverage table has all 21 v1.2 requirements SATISFIED with cross-references to evidence (Phase 13/14/15 VERIFICATION.md + 16-LIVE-VERIFICATION.md).
- [ ] Phase Verification Summary has rows for all four phases with must-have counts sourced from the per-phase VERIFICATION.md files.
- [ ] E2E Flows section lists fake / live / graceful-degradation paths with evidence citations.
- [ ] Tech Debt Register is either empty (→ status: shipped) or populated with all-Low severity items including any Tier-B VOICE-01 deferrals from 16-LIVE-VERIFICATION.md (→ status: tech_debt, acceptable).
- [ ] Anti-Patterns Scan reports zero TODO/FIXME markers in v1.2 load-bearing code (confirmed by grep).
- [ ] Test Suite Evidence cites actual backend pytest + frontend vitest + build-success numbers as of 16-01 Task 1 exit.
- [ ] Human Verification Evidence cites Phase 14 VLC fake, Phase 15 garbage-key banner screenshots + markdown SHA, Phase 16 VLC real listen-through + deviation table + player screenshot.
- [ ] REQUIREMENTS.md footer has a new dated `*Last updated:...*` line citing `16-LIVE-VERIFICATION.md` and noting v1.2 milestone audited; no new column introduced; all existing row statuses preserved.
- [ ] PROJECT.md Key Decisions table has a new v1.2-milestone-shipped entry matching v1.1 precedent shape.
- [ ] ROADMAP.md top Milestones section shows v1.2 `✅` with shipped date + link to the audit; Phase 16 entry is `[x]`; both plans 16-01 and 16-02 are `[x]`; Progress table row 16 shows `2/2 | Complete | [date]`.
- [ ] STATE.md Current Position is Phase 16/16 Plan 2/2; progress bar 58/58; Shipped Milestones row for v1.2 is `✅ Tagged`.
- [ ] All cross-file links resolve — no 404s between the audit, REQUIREMENTS.md, ROADMAP.md, STATE.md, and PROJECT.md.
- [ ] All five file edits committed; the v1.2 milestone has formally shipped.

</success_criteria>

<output>
After completion, create `.planning/phases/16-live-elevenlabs-verification/16-02-SUMMARY.md` following the standard summary template:

- Frontmatter: `phase`, `plan`, `shipped_date`, `status: complete`, `subsystem: audit+docs`, `tech-stack.added: []`, `affects: []` (terminal plan), `requires: [16-01]`.
- Key decisions (expect 2-4): final audit `status` (shipped vs tech_debt) with justification; list of any v1.3 VOICE-01 backlog items seeded by Tier-B deferrals; inherited-debt closure note for TD-v1.1-01.
- Key files: the five files touched by this plan.
- Verification summary: SC4 verdict with one-line justification.
- Handoff to next milestone (v1.3 or beyond): explicit "v1.2 shipped on [date]. Any Tier-B voice observations captured in the Tech Debt Register of v1.2-MILESTONE-AUDIT.md are ready inputs for v1.3 VOICE-01. No open blockers."
</output>
