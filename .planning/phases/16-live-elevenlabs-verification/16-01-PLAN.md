---
phase: 16-live-elevenlabs-verification
plan: 01
plan_id: "16-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/config_tts.py
  - backend/app/main.py
  - backend/tests/test_config_tts.py
  - src/components/game/FacilitatorInput/ActionToolbar.tsx
  - src/lib/ttsVoicesClient.ts
  - src/lib/__tests__/ttsVoicesClient.test.ts
  - .planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json
  - .planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py
  - .planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3
  - .planning/phases/16-live-elevenlabs-verification/evidence/segment-offsets.json
  - .planning/phases/16-live-elevenlabs-verification/evidence/player-screenshot.png
  - .planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md
autonomous: false
requirements:
  re_exercised:
    - PODGEN-01
    - PODGEN-02
    - PODGEN-03
    - PODGEN-04
    - PODGEN-05
    - PODGEN-06
    - PODGEN-07
    - PODGEN-08
    - PODPLAY-01
    - PODPLAY-02
    - PODPLAY-03
    - PODPLAY-04
    - PODPLAY-05
    - PODUX-01
    - PODUX-02
    - PODUX-03
user_setup:
  - service: elevenlabs
    why: "Phase 16 calls real ElevenLabs API to produce audible MP3 evidence"
    env_vars:
      - name: TTS_PROVIDER
        source: "backend/.env (set to 'elevenlabs' for this replay)"
      - name: ELEVENLABS_API_KEY
        source: "ElevenLabs dashboard -> Profile -> API Key (operator-held; never committed)"
      - name: ELEVENLABS_VOICE_KENT
        source: "ElevenLabs dashboard -> VoiceLab -> v0.10 locked Kent voice ID"
      - name: ELEVENLABS_VOICE_FINCH
        source: "ElevenLabs dashboard -> VoiceLab -> v0.10 locked Finch voice ID"
      - name: ELEVENLABS_VOICE_CHEN
        source: "ElevenLabs dashboard -> VoiceLab -> v0.10 locked Chen voice ID"
    dashboard_config:
      - task: "Confirm account has >= 1,000 chars of remaining quota before replay (Scenario-2 fixture is ~240 chars)"
        location: "ElevenLabs dashboard -> Subscription / Usage"
must_haves:
  truths:
    - "Backend booted with TTS_PROVIDER=elevenlabs accepts POST /api/debrief/podcast with real voice IDs and returns a token whose GET /api/debrief/podcast/audio?token=... yields audio/mpeg bytes"
    - "GET /api/config/tts-voices returns {kent, finch, chen} voice IDs sourced from settings.elevenlabs_voice_* when TTS_PROVIDER=elevenlabs, and returns the sentinel strings (__fake_kent__ etc.) when TTS_PROVIDER=fake"
    - "ActionToolbar.tsx no longer hardcodes sentinel voice IDs in the Generate path; voices come from GET /api/config/tts-voices"
    - "Scenario-2 debrief fixture is locked at fixtures/scenario2-debrief.json with three persona text fields matching 08-02-DEBRIEF-export.md lines 118-122 verbatim"
    - "evidence/debrief-scenario2-live.mp3 is committed, SHA-256 recorded in 16-LIVE-VERIFICATION.md, and plays end-to-end in VLC with three distinct voices in Kent -> Finch -> Chen order"
    - "evidence/segment-offsets.json contains [0.0, finch_start_s, chen_start_s] computed from CBR mp3_44100_128 formula and matches what the backend SSE done event returned"
    - "evidence/player-screenshot.png shows PodcastPlayer in loaded-paused state with <audio controls>, three Skip buttons, Now playing label, and Download MP3 button all visible"
    - "16-LIVE-VERIFICATION.md contains all 6 sections (metadata, payload, response evidence, stitched-MP3 meta, acronym deviation table for all 8 acronyms, verdict + cross-references) with no [TBD] placeholders"
    - "No file in .planning/phases/16-live-elevenlabs-verification/ contains the literal ELEVENLABS_API_KEY value or the unredacted voice ID strings"
    - "All 8 target acronyms (EDIP, PC, PO, CRM, IC, LEFS, SIEP, SoS) have a row in the deviation table with term/expected/heard/tier/disposition filled in"
  artifacts:
    - path: "backend/app/routers/config_tts.py"
      provides: "GET /api/config/tts-voices endpoint"
      exports: ["router"]
      min_lines: 30
    - path: "backend/tests/test_config_tts.py"
      provides: "pytest coverage for config_tts router (fake + elevenlabs branches)"
      min_lines: 40
    - path: "src/lib/ttsVoicesClient.ts"
      provides: "fetchTtsVoices() -> Promise<{kent, finch, chen}>"
      exports: ["fetchTtsVoices"]
    - path: ".planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json"
      provides: "locked Scenario-2 debrief persona texts"
      contains: '"kent":'
    - path: ".planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py"
      provides: "standalone Tier-B replay script modeled on run_firewall_spike.py"
      min_lines: 150
    - path: ".planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3"
      provides: "stitched three-voice live MP3 committed as binary evidence"
    - path: ".planning/phases/16-live-elevenlabs-verification/evidence/segment-offsets.json"
      provides: "per-segment offsets JSON emitted by backend done event"
      contains: '"offsets"'
    - path: ".planning/phases/16-live-elevenlabs-verification/evidence/player-screenshot.png"
      provides: "loaded-paused PodcastPlayer screenshot"
    - path: ".planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md"
      provides: "Tier-B evidence record mirroring 12-LIVE-VERIFICATION.md shape"
      min_lines: 120
  key_links:
    - from: "src/components/game/FacilitatorInput/ActionToolbar.tsx"
      to: "/api/config/tts-voices"
      via: "fetchTtsVoices() call in handleGenerate before startPodcast"
      pattern: "fetchTtsVoices"
    - from: "backend/app/routers/config_tts.py"
      to: "settings.elevenlabs_voice_kent/finch/chen"
      via: "get_settings() in the endpoint handler"
      pattern: "elevenlabs_voice_"
    - from: "backend/app/main.py"
      to: "backend/app/routers/config_tts.py"
      via: "include_router(config_tts.router) registered BEFORE the SPA static mount"
      pattern: "include_router\\(config_tts"
    - from: ".planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py"
      to: "http://localhost:8000/api/debrief/podcast"
      via: "requests.post with SSE streaming consumer"
      pattern: "/api/debrief/podcast"
    - from: ".planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md"
      to: ".planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3"
      via: "Section 4 stitched-MP3 SHA-256 + relative link to binary"
      pattern: "debrief-scenario2-live\\.mp3"
---

<objective>
Produce the Tier-B evidence bundle that proves PODGEN-01..08 and PODPLAY-01..05 hold against a real ElevenLabs API key using the v0.10 configured voice IDs, a locked Scenario-2 debrief fixture, and a committed stitched MP3. Bundled with this evidence work is the one blocking code-path fix identified in 16-RESEARCH.md Section 4: the ActionToolbar.tsx sentinel voice IDs (`__fake_kent__` etc.) are replaced with a new `GET /api/config/tts-voices` endpoint + frontend client so the browser listen-through path posts real voice IDs.

Purpose: Close roadmap SC1 (stitched MP3 plays end-to-end in VLC), SC2 (acronym pronunciation tiered verdict), and SC3 (evidence bundle completeness). Without this plan's evidence, the v1.2 milestone audit in 16-02 has nothing to cite for the live-validation claim — Phases 13/14/15 all ran against FakeTTSProvider only.

Output: committed `16-LIVE-VERIFICATION.md` + `fixtures/scenario2-debrief.json` + `evidence/debrief-scenario2-live.mp3` + `evidence/segment-offsets.json` + `evidence/player-screenshot.png` + new `config_tts` router + updated `ActionToolbar.tsx`, all mirroring the `12-LIVE-VERIFICATION.md` / `run_firewall_spike.py` v1.1 precedents exactly.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/16-live-elevenlabs-verification/16-CONTEXT.md
@.planning/phases/16-live-elevenlabs-verification/16-RESEARCH.md

# v1.1 Tier-B precedent — shape of the evidence record this plan produces
@.planning/phases/12-crisis-state-prompt-engineering/12-LIVE-VERIFICATION.md

# Reference implementation for auth-hygiene + env-var discipline in standalone script
@.planning/phases/13-firewall-spike-mockable-backend-foundation/scripts/run_firewall_spike.py

# Scenario-2 debrief fixture source (lines 118-122 are the verbatim persona texts)
@.planning/phases/08-qa-credential-audit/08-02-DEBRIEF-export.md

# Code touched by the bundled sentinel-voice-ID fix
@src/components/game/FacilitatorInput/ActionToolbar.tsx
@backend/app/config.py
@backend/app/routers/debrief.py
@backend/app/main.py
@backend/app/services/audio_generator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend + frontend wiring — GET /api/config/tts-voices endpoint replaces sentinel voice IDs</name>
  <files>
    backend/app/routers/config_tts.py
    backend/app/main.py
    backend/tests/test_config_tts.py
    src/lib/ttsVoicesClient.ts
    src/lib/__tests__/ttsVoicesClient.test.ts
    src/components/game/FacilitatorInput/ActionToolbar.tsx
  </files>
  <action>
    Implement the one blocking code change identified by 16-RESEARCH.md Section 4 and pitfall P1: the browser-path `handleGenerate` in `ActionToolbar.tsx` lines 63-68 currently hardcodes `__fake_kent__ / __fake_finch__ / __fake_chen__` sentinel strings. When `TTS_PROVIDER=elevenlabs`, those sentinels 404 at ElevenLabs, so the human listen-through via the UI cannot work. Fix by introducing a backend config endpoint + tiny frontend client.

    **Backend — `backend/app/routers/config_tts.py` (NEW FILE):**
    - Create `router = APIRouter()` with one endpoint: `@router.get("/api/config/tts-voices")`.
    - Handler reads `settings = get_settings()`. If `settings.tts_provider == "elevenlabs"`, return `{"kent": settings.elevenlabs_voice_kent, "finch": settings.elevenlabs_voice_finch, "chen": settings.elevenlabs_voice_chen}` with HTTP 200. The four `ELEVENLABS_*` vars are already validated non-empty by the `validate_elevenlabs_config` model-validator in `backend/app/config.py:90-107`, so no defensive None-check is needed here — if the backend is up with `TTS_PROVIDER=elevenlabs`, the values are guaranteed present.
    - If `settings.tts_provider == "fake"`, return `{"kent": "__fake_kent__", "finch": "__fake_finch__", "chen": "__fake_chen__"}`. This preserves existing Phase 14 fake-mode behaviour verbatim so no tests regress.
    - Log nothing sensitive. Do NOT log the voice ID values — per 16-CONTEXT.md redaction hygiene, the real IDs must never appear in server logs either (stdout can be captured in CI).

    **Backend — `backend/app/main.py`:**
    - Import: `from .routers import config_gen, config_tts, debrief, health, health_tts, llm` (insert `config_tts` alphabetically).
    - Register: add `app.include_router(config_tts.router)` after the existing `app.include_router(health_tts.router)` line (around `main.py:119`). **CRITICAL:** the `include_router` call MUST land BEFORE the `SPAStaticFiles` mount at `main.py:126` — the SPA catch-all otherwise swallows the `/api/config/tts-voices` path and serves `index.html`. Match the precedent established by `health_tts.router` registration.

    **Backend tests — `backend/tests/test_config_tts.py` (NEW FILE):**
    - Use the existing pytest FastAPI test-client pattern (see `backend/tests/test_health_tts.py` for the reference shape — `env_tts_elevenlabs` and `env_tts_fake` conftest fixtures already exist).
    - Write exactly 3 tests:
      1. `test_returns_sentinels_when_tts_provider_fake`: with `TTS_PROVIDER=fake`, GET `/api/config/tts-voices` returns `{"kent": "__fake_kent__", "finch": "__fake_finch__", "chen": "__fake_chen__"}`.
      2. `test_returns_configured_ids_when_tts_provider_elevenlabs`: with `TTS_PROVIDER=elevenlabs` + `ELEVENLABS_API_KEY=test` + three `ELEVENLABS_VOICE_*` values set via the `env_tts_elevenlabs` fixture, GET returns those three values verbatim in the response body.
      3. `test_response_shape_stable`: response is exactly three string keys `kent`, `finch`, `chen` — no extra fields. This is the contract the frontend client depends on.
    - Do NOT use real voice IDs. Use stub strings like `"voice_kent_stub"` etc. — the test fixture is not the live replay.

    **Frontend — `src/lib/ttsVoicesClient.ts` (NEW FILE):**
    - Export `async function fetchTtsVoices(): Promise<{ kent: string; finch: string; chen: string }>`.
    - Implementation: `const res = await fetch('/api/config/tts-voices'); if (!res.ok) throw new Error(\`Failed to fetch TTS voices: HTTP \${res.status}\`); return res.json();`.
    - Add a TypeScript interface `TtsVoiceMap` for the response shape; export it.

    **Frontend tests — `src/lib/__tests__/ttsVoicesClient.test.ts` (NEW FILE):**
    - Follow the existing vitest+msw pattern used in `src/lib/podcastClient.test.ts` (or plain `vi.stubGlobal('fetch', ...)` — match whichever pattern the existing `src/lib` tests use).
    - Write exactly 2 tests:
      1. `returns parsed voice map on HTTP 200` — mock fetch to return `{kent: 'a', finch: 'b', chen: 'c'}`, assert the function resolves to that exact object.
      2. `throws on HTTP non-2xx` — mock fetch to return status 500, assert the function rejects with an Error containing `"HTTP 500"`.

    **Frontend — `src/components/game/FacilitatorInput/ActionToolbar.tsx` lines 53-75 + line 212:**
    - Add import at top: `import { fetchTtsVoices } from '@/lib/ttsVoicesClient'`.
    - Convert `handleGenerate` to async (it's currently synchronous). Replace the hardcoded `voices` object (lines 63-68) with `const voices = await fetchTtsVoices()`. Place this call AFTER the word-count gate so a confirmation-dialog open does not incur an unneeded fetch. Adjust the `onClick={() => handleGenerate(false)}` call sites — `handleGenerate` can now return a Promise; callers ignore the return value via `void handleGenerate(false)`.
    - Remove the `// Fake-mode sentinels — real voice IDs arrive in Phase 16 via settings` comment since the sentinel-vs-real dispatch is now server-side.
    - Line 212 (inside the `RegenerateConfirmDialog` onConfirm handler): the same sentinel strings appear there for the force-fresh path. Apply the same `await fetchTtsVoices()` refactor.
    - Handle `fetchTtsVoices` rejection: if the fetch throws, surface it through the existing podcast-store error path — call `usePodcastStore.getState().setError({ code: 'network_error', status: null, hint: 'Could not fetch TTS voice configuration' })` (or whichever error-setter the store already exposes; match its signature from `src/lib/podcastStore.ts`). The UI already renders store errors into the error banner, so no new UI surface is required.
    - Update any existing `ActionToolbar` vitest test that asserted the hardcoded sentinel strings: mock `fetchTtsVoices` to resolve with `{kent: '__fake_kent__', finch: '__fake_finch__', chen: '__fake_chen__'}` so existing assertions still hold in fake mode.

    **Why these tests and not more:** This is an infrastructure wiring task, not a product feature. Three backend tests (fake-branch / elevenlabs-branch / shape-contract) + two frontend tests (happy path / error path) fully cover the endpoint surface. Do not add end-to-end tests — the Tier-B replay in Task 2 is the integration proof.
  </action>
  <verify>
    - `cd backend && pytest tests/test_config_tts.py -v` → 3/3 tests pass.
    - `cd backend && pytest` → full backend suite still green (Phase 15 exit state was 139 tests; after this task expect ~142).
    - `npm run test -- src/lib/__tests__/ttsVoicesClient.test.ts` → 2/2 pass.
    - `npm run test` → full frontend suite still green (Phase 15 exit state was 625; expect 627+ after this task).
    - `npm run build` → `tsc -b && vite build` succeeds with no new type errors.
    - Start backend with `TTS_PROVIDER=fake` (dev default): `curl http://localhost:8000/api/config/tts-voices` returns `{"kent":"__fake_kent__","finch":"__fake_finch__","chen":"__fake_chen__"}`.
    - Grep for the literal string `__fake_kent__` in `src/components/game/FacilitatorInput/ActionToolbar.tsx` → zero matches (the sentinel is now server-side only).
    - `grep -rn "include_router(config_tts" backend/app/main.py` → exactly one match, and its line number is BEFORE any `.mount(` line in that file.
  </verify>
  <done>
    New `config_tts` router exists, is registered before the SPA mount, returns sentinels in fake mode and real IDs in elevenlabs mode, has 3 passing pytest tests. `ActionToolbar.tsx` no longer contains hardcoded sentinel voice IDs; both Generate and Re-generate paths fetch voices from the new endpoint. Frontend has 2 passing vitest tests for the client. Full test suites (backend + frontend) remain green. Browser-path live test is now unblocked — POSTs will carry real voice IDs when the backend is booted with `TTS_PROVIDER=elevenlabs`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock Scenario-2 fixture, author replay script, execute live replay, capture evidence bundle</name>
  <files>
    .planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json
    .planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py
    .planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3
    .planning/phases/16-live-elevenlabs-verification/evidence/segment-offsets.json
  </files>
  <action>
    Produce the raw-bytes evidence. This task captures everything the subsequent human listen-through needs.

    **Sub-step A — Lock the fixture.**

    Create `.planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json` with the three persona texts verbatim from `.planning/phases/08-qa-credential-audit/08-02-DEBRIEF-export.md` lines 118-122. Use the exact JSON shape specified in 16-RESEARCH.md Section 6:

    ```json
    {
      "kent": "As we conclude the game, it's crucial to reflect on the effectiveness of EDIP tools in managing supply crises and maintaining legitimacy. Understanding which tools balance resilience and political acceptability will inform future strategic frameworks.",
      "finch": "Throughout the simulation, the adaptability of our responses to high-intensity conflict and shifting crisis states provided insights into potential real-world applications. The implications of mandatory measures on EDIP legitimacy require careful consideration going forward.",
      "chen": "Evaluating operational readiness and the flow of resources offers a pragmatic view of the challenges encountered. Balancing immediate demands with sustainable practices will be key to refining future EDIP mechanisms."
    }
    ```

    Do NOT edit, paraphrase, truncate, or "clean up" the texts. The fixture is locked as captured.

    **Sub-step B — Author the replay script.**

    Create `.planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py`. Model its structure on `.planning/phases/13-firewall-spike-mockable-backend-foundation/scripts/run_firewall_spike.py`:

    - Shebang + docstring explaining: purpose (Tier-B replay of live ElevenLabs against Scenario-2 fixture), usage (run ON the deployment host with backend already booted as `TTS_PROVIDER=elevenlabs` on `http://localhost:8000`), exit codes (0 = success, 1 = HTTP/validation failure, 2 = env-var missing or fixture malformed).
    - Dependencies: `requests` + `sseclient-py` (the `requests` SSE consumer the firewall spike uses `requests` alone; if `sseclient` isn't available, roll a minimal line-by-line SSE parser inline since the SSE framing is simple `data: {json}\n\n`). Prefer the inline parser — one less external dep.
    - Env-var guard at top: `ELEVENLABS_API_KEY` must be set (the backend already validated it at startup; the script re-checks so the script output isn't misleading if the backend is misconfigured). Do NOT require the voice IDs in the script env — the script fetches them from `GET /api/config/tts-voices` so the real IDs never have to cross this script's env surface.
    - `_SENSITIVE_HEADER_FRAGMENTS` constant copied verbatim from `run_firewall_spike.py:62-68`. The `_sanitize_headers(headers)` helper strips those before printing.
    - Reads `fixtures/scenario2-debrief.json` and validates shape: three string keys `kent/finch/chen`, all non-empty. Exit 2 if malformed.
    - GETs `http://localhost:8000/api/config/tts-voices` → parse `{kent, finch, chen}`. **CRITICAL:** do NOT print the response body values (they are real voice IDs). Print only `"Fetched 3 voice IDs from /api/config/tts-voices (values redacted)"`.
    - Preflight: GET `http://localhost:8000/api/health/tts` → if `body.ok !== true`, exit 1 with the `body.code` and `body.hint`. This catches quota exhaustion and auth-config errors before the POST burns any ElevenLabs budget (16-RESEARCH.md pitfall P5).
    - POST `http://localhost:8000/api/debrief/podcast` with JSON body `{"game_name": "scenario2-debrief-live-replay", "persona_texts": <fixture>, "voices": <voice_map>, "force_fresh": true}`. `Accept: text/event-stream`, no other headers. The backend reads `ELEVENLABS_API_KEY` from its own env; the script does NOT forward the key.
    - SSE parser: read `response.iter_lines()`, buffer `data: ...` lines until blank-line event delimiter, JSON-parse each event. Capture every `persona_done` event (note persona name, timestamp) and the final `done` event (contains `{token, offsets, word_count}`). Also log `persona_start` events with per-event latency timing. If an `error` event arrives, exit 1 with the `code` and `hint`.
    - Retry policy per 16-CONTEXT.md: if the initial POST fails with HTTP 5xx, network reset, or a transient read timeout, retry up to 3 times with exponential backoff 1s → 2s → 4s. If HTTP 401 / 403 / 429 comes back at any point, exit 1 immediately with a clear message — those are environmental blockers, not transient failures. On a preprocessor-miss-detected scenario (Tier A re-run), operator can invoke the script with `--persona kent|finch|chen` to run a single-persona re-gen; the script skips the other two slots in the POST and splices the returned segment into the existing MP3 by re-stitching at the known offset. **After splicing, the script recomputes `evidence/segment-offsets.json` from the new segment byte boundaries using the CBR formula (`seconds = (byte_count * 8) / 128000`) and overwrites the file — ElevenLabs output length is not byte-deterministic for the same input, so stale offsets would cause Skip-to-Finch/Chen to land in the wrong segment and PODPLAY-04 evidence would be wrong.** For the FIRST full run, no `--persona` flag is needed.
    - GET `http://localhost:8000/api/debrief/podcast/audio?token=<token>` within 30s of the `done` event (the TokenStore TTL is 60s per `audio_generator.py:31`; 30s gives 30s headroom). Response must be `Content-Type: audio/mpeg`. Write bytes to `.planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3`. Compute and print SHA-256 of the bytes.
    - Write the `done` event's `offsets` field to `.planning/phases/16-live-elevenlabs-verification/evidence/segment-offsets.json`. Shape: `{"offsets": [0.0, finch_start_s, chen_start_s], "word_count": <int>, "captured_at": "<ISO-8601>"}`.
    - Print summary block to stdout (this is the text the operator pastes into `16-LIVE-VERIFICATION.md` Section 3):
      - Per-segment: persona name, HTTP-200 confirmation, `Content-Type` (expect `audio/mpeg`), total SSE-stream latency from POST to `persona_done`, first 32 bytes of each segment as hex. Segment bytes are NOT captured individually by the SSE stream (the backend only emits the final stitched MP3 via the token endpoint), so per-segment byte inspection uses the offsets to slice the stitched MP3 bytes at the end: bytes `[0 : finch_offset_bytes]` = Kent, `[finch_offset_bytes : chen_offset_bytes]` = Finch, `[chen_offset_bytes : end]` = Chen. Use the same `bytes_to_seconds = (n * 8) / 128000` CBR formula from `audio_generator.py` inverted: `seconds_to_bytes(s) = int(s * 128000 / 8)`.
      - Stitched MP3: total byte count, expected-size sanity check (~120KB-600KB for ~80-word total), SHA-256, first 32 bytes hex (should be an MP3 frame header like `fffb90...` or a Xing/VBRI header on first frame).
      - Total replay wall-clock time.
    - Auth-leak assertion before exit: scan the complete stdout buffer for any substring matching the `ELEVENLABS_API_KEY` env value or any of the three voice ID values from the `/api/config/tts-voices` response. If ANY match, dump a big red `AUTH_LEAK_DETECTED` line to stderr and exit 1 — this is the discipline from `run_firewall_spike.py:200-205` adapted for Phase 16's larger auth surface.
    - Exit 0 on success.

    The script's length will be ~200 lines — comparable to `run_firewall_spike.py`. Do NOT add `requests` to `backend/requirements.txt`; the script is evidence-capture tooling, not production code. Operator installs `pip install requests` once on the target host (same precedent as Phase 13).

    **Sub-step C — Execute the live replay.**

    On the deployment host `MC211APT2AS5AHG` (per 16-RESEARCH.md Section 7):

    1. Confirm `backend/.env` has `TTS_PROVIDER=elevenlabs` + non-empty `ELEVENLABS_API_KEY` + non-empty `ELEVENLABS_VOICE_KENT/FINCH/CHEN`. If any is missing, the backend will fail to start with the `validate_elevenlabs_config` ValueError (config.py:90-107). Fix out-of-band and retry. DO NOT commit the `.env`.
    2. Start backend: `cd backend && uvicorn app.main:app --host 127.0.0.1 --port 8000`. Confirm no startup errors. The Phase 13/15 existing uvicorn startup pattern works as-is.
    3. Install replay-script deps: `pip install requests` (one-time).
    4. Run the script: `python .planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py`.
    5. Script exits 0 → evidence MP3 + offsets JSON land in `evidence/`. Save stdout to a temp file for Section 3 of the evidence record (Task 3).
    6. Script exits 1 on transient → re-run up to 3 times total (matches the in-script retry policy, provides an outer retry for the one-shot GET audio fetch).
    7. Script exits 1 on 401/403/429 → STOP. Document the environmental blocker in 16-LIVE-VERIFICATION.md Section 5 as a ship-blocker, do not retry. Escalate to operator.
    8. Script exits 2 → fixture or env malformed; fix input, re-run.

    **Sub-step D — Commit evidence.**

    Stage: `fixtures/scenario2-debrief.json`, `scripts/run_live_replay.py`, `evidence/debrief-scenario2-live.mp3` (binary), `evidence/segment-offsets.json`. Commit message: `evidence(16-01): Tier-B live ElevenLabs replay — Scenario-2 debrief MP3 + offsets`. The binary MP3 commit is a one-time evidence commit following the v1.1 Tier-B raw-bytes pattern (precedent: `.planning/phases/13-firewall-spike-mockable-backend-foundation/13-firewall-spike-payload.mp3` is committed as binary evidence the same way).

    **Do NOT commit:** `backend/.env`, the captured script stdout (goes into 16-LIVE-VERIFICATION.md Section 3 only, redacted), any file containing the raw API key or raw voice IDs.
  </action>
  <verify>
    - `.planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json` exists, JSON-parses, has exactly three string keys (`kent`, `finch`, `chen`) each non-empty, and texts character-match `.planning/phases/08-qa-credential-audit/08-02-DEBRIEF-export.md` lines 118-122 verbatim.
    - `python -m json.tool .planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json` succeeds.
    - `.planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py` exists, has a shebang + module docstring, and `python -c "import ast; ast.parse(open(r'.planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py').read())"` succeeds (no syntax errors).
    - `grep -E "xi-api-key|authorization|Bearer " .planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py` shows these strings ONLY inside the `_SENSITIVE_HEADER_FRAGMENTS` redaction list or comments — never in a header-construction call.
    - `.planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3` exists, size is between 80,000 and 1,200,000 bytes (sanity envelope for ~80-word total at 128kbps CBR), first 2 bytes are `0xFF 0xFB` or `0xFF 0xF3` (MPEG frame-sync prefix) or contains a Xing/VBRI/Info header in the first frame.
    - `file .planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3` reports an MPEG ADTS / Layer III audio file.
    - `.planning/phases/16-live-elevenlabs-verification/evidence/segment-offsets.json` JSON-parses, has `offsets` field with exactly 3 numeric elements in strictly increasing order starting with `0.0`, and the second and third elements are both > 0 and < the total MP3 duration computed from `(file_size * 8) / 128000`.
    - `grep -rE "ELEVENLABS_API_KEY[= ]|xi-api-key: [^R]|voice_id: [a-zA-Z0-9]{20,}" .planning/phases/16-live-elevenlabs-verification/` → no matches outside docstrings that refer to env var *names* (not values).
    - Running `sha256sum .planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3` produces a hex digest (will be captured verbatim in Task 3's Section 4).
  </verify>
  <done>
    Scenario-2 fixture is locked, replay script is committed and syntactically valid, live replay executed successfully against the real ElevenLabs endpoint from the deployment host, and the three evidence artifacts (stitched MP3 binary, offsets JSON, fixture JSON) are committed. No API key or voice ID values appear in any committed file. Replay-script stdout has been captured to a scratch file for Task 3's evidence-record authoring.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Human VLC listen-through + deviation-table completion + player screenshot + author 16-LIVE-VERIFICATION.md</name>
  <files>
    .planning/phases/16-live-elevenlabs-verification/evidence/player-screenshot.png
    .planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md
  </files>
  <what-built>
    Task 1 shipped the `/api/config/tts-voices` endpoint + ActionToolbar fix. Task 2 produced the binary evidence artifacts. This task is the **human-gated verification** that the evidence actually proves Roadmap SC1 (audible MP3 end-to-end) and SC2 (acronym pronunciation), plus authorship of the Tier-B evidence record that ties everything together.

    Claude cannot listen to audio, cannot open VLC, and cannot judge subjective pronunciation quality. The operator does steps 1-3 below; Claude drafts 16-LIVE-VERIFICATION.md in step 4 from the operator's deviation-table input + Task 2's captured stdout.
  </what-built>

  <how-to-verify>
    **Step 1 — VLC listen-through (operator, ~3 minutes).**

    Follow 16-RESEARCH.md Section 8 exactly:

    1. Open `.planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3` in VLC (File → Open File).
    2. Press Play. Do NOT skip. Listen all the way through.
    3. Note VLC's reported total duration. Expected: roughly 25-40 seconds (Kent ~8-12s + 0.7s pad + Finch ~9-14s + 0.7s pad + Chen ~7-11s). Chrome/VLC will show the correct duration; WMP may show only the first-frame duration — known cosmetic quirk from 14-03 decisions, not a defect.
    4. Confirm three distinct voices are audible in Kent → Finch → Chen order. Cadence and timbre should clearly differ between segments. If any two voices sound identical or the order is wrong, that's a Tier C issue — see "If SC1 fails" below.
    5. During each segment, track every target acronym from the 8-item set:
       - Kent's segment: listen for `EDIP`.
       - Finch's segment: listen for `EDIP`.
       - Chen's segment: listen for `EDIP`.
       - (Note: The Scenario-2 fixture at `fixtures/scenario2-debrief.json` only contains `EDIP` — the other 7 acronyms from the 8-item canonical set (PC, PO, CRM, IC, LEFS, SIEP, SoS) are NOT present in this particular debrief capture. The deviation table in Section 5 of the evidence record MUST still include all 8 acronyms with rows — acronyms not present in this fixture get `heard: N/A — not in fixture` / `tier: N/A` / `disposition: golden-corpus coverage (already validated in 13-VERIFICATION.md per preprocessor golden_corpus.json)`. This is the v1.1 precedent for citing prior evidence for items not empirically re-exercised in the current phase.)

    **Step 2 — Capture deviation table input (operator, paste into structured text).**

    For each of the 8 target acronyms, record in your notes:
    - `term`: the acronym string (e.g., `EDIP`)
    - `expected pronunciation`: per `backend/app/services/text_preprocessor.py` ACRONYMS dict (16-RESEARCH.md Section 5) — e.g., `E D I P` = "ee-dee-eye-pee" letter-by-letter
    - `heard`: what you actually heard — verbatim, even if it sounds wrong ("ee-dip", "eedip", etc.)
    - `tier`: classify per 16-CONTEXT.md "Listen-through deviation policy":
      - `A` → preprocessor miss, dict-fixable; stops Task 4 ship gate until the Tier-A re-run loop completes.
      - `B` → stock-voice limitation; defer to v1.3 VOICE-01; does not block.
      - `C` → structural defect (wrong text, corrupted audio, cuts off); STOPS milestone. Escalate immediately.
      - `N/A` → not in fixture; cite prior golden-corpus coverage.
      - `PASS` → heard correctly.
    - `disposition`: one sentence — either "PASS", "Tier-A preprocessor dict update applied in Task 3.5 re-run", "deferred-to-VOICE-01", "N/A — not in fixture, covered by preprocessor_golden.json", or (Tier C) "BLOCKER — milestone halted pending Phase 17 insert".

    **Step 2.5 — Tier-A fix loop (operator, only if any row = tier A).**

    If any acronym row is Tier A:
    1. Follow the Tier-A fix procedure in 16-RESEARCH.md Section 5: add the dict entry to `backend/app/services/text_preprocessor.py`, add a new golden-file case to `backend/tests/fixtures/preprocessor_golden.json`, run `cd backend && pytest tests/test_preprocessor.py -v` and confirm all tests pass.
    2. Re-run the replay script with `--persona <name>` for ONLY the affected persona (the script supports single-persona re-gen per Task 2's sub-step B spec). The replay script stitches the corrected segment into the existing MP3 using the recorded offsets.
    3. Re-do the VLC listen-through on the corrected segment only.
    4. The deviation table in the evidence record gets TWO rows for that acronym: "before-fix" (tier A, with disposition "Tier-A preprocessor dict update — see commit <sha>") and "after-fix" (tier PASS, with disposition "PASS — preprocessor dict entry added"). This matches the v1.1 precedent of showing both the initial miss and the correction as evidence.
    5. Commit the preprocessor + golden-corpus changes separately: `fix(16-01): Tier-A preprocessor dict update for <acronym> — surfaced by live listen-through`.

    **Step 3 — Player screenshot (operator).**

    1. In the browser (NOT in VLC — this is the app-UI screenshot), run a fresh game end-to-end with `TTS_PROVIDER=elevenlabs` against the Scenario-2 fixture. Use the Task-1 `GET /api/config/tts-voices` path (so real voice IDs are posted by the browser — this is the integration proof that the Task 1 fix works end-to-end).
    2. Once the player loads, pause it BEFORE pressing play (or press pause within the first half-second). The `<audio controls>` element is seeded paused per PODPLAY-01 — it should naturally be in the paused state when it first appears.
    3. Take one screenshot showing: the `<audio controls>` element, the three Skip buttons (Skip to Kent / Finch / Chen), the "Now playing" label, and the Download MP3 button all visible in one frame.
    4. Save to `.planning/phases/16-live-elevenlabs-verification/evidence/player-screenshot.png`.
    5. If a second player-state screenshot helps (e.g., mid-playback showing Now-playing updated) feel free to capture it too, but loaded-paused is the canonical proof per 16-CONTEXT.md "Claude's Discretion".

    **Step 4 — Resume Claude with deviation-table input.**

    Paste back to Claude: the complete deviation table (8 rows — or more if Tier-A double-rows), the VLC-reported total duration, the verdict (PASS/FAIL on SC1 end-to-end, tiered verdict on SC2 acronyms), and the replay-script stdout captured in Task 2 sub-step C step 5. Claude then authors `16-LIVE-VERIFICATION.md`.

    **Step 4 Claude authoring spec for `16-LIVE-VERIFICATION.md`** (Claude executes this after resume signal):

    Use the section shape from `.planning/phases/12-crisis-state-prompt-engineering/12-LIVE-VERIFICATION.md` exactly — no YAML frontmatter (v1.1 precedent), plain Markdown sections numbered 1-6:

    - **Opening prose paragraph** (no section number): one paragraph mirroring `12-LIVE-VERIFICATION.md` line 1-5. State: this is the Phase 16 Tier-B empirical replay against real ElevenLabs using the Scenario-2 debrief fixture, closing Phase 16 SC1 + SC2 + SC3, with the bundled `GET /api/config/tts-voices` fix that unblocks the browser path.

    - **§1 Replay metadata** — two-column table:
      - Replay date: ISO-8601 of run
      - Deployment host: `MC211APT2AS5AHG`
      - Git SHA carrying Task 1 code changes: capture via `git rev-parse HEAD` at end of Task 1 commit
      - ElevenLabs endpoint: `api.elevenlabs.io` (redact any model/voice-specific path details that leak voice IDs; just the host)
      - `TTS_PROVIDER` env value: `elevenlabs`
      - `ELEVENLABS_API_KEY` presence: "confirmed non-empty (value redacted)" — **never write the literal key**
      - Voice IDs source: "backend `.env` (deployment-host, gitignored); values fetched at replay time via `/api/config/tts-voices`, values redacted throughout this record"
      - Model ID: `eleven_multilingual_v2` (from `config.py:77` default)
      - Output format: `mp3_44100_128` (from `config.py:78` default)
      - Backend test state: "142+/142+ pytest green" (post-Task 1 — exact count from Task 1 verify step)
      - Frontend test state: "627+/627+ vitest green" (post-Task 1 — exact count from Task 1 verify step)
      - Replay-script SHA: `sha256sum` of `scripts/run_live_replay.py`

    - **§2 Replay payload** — three subsections:
      - **§2a Locked fixture texts:** embed all three persona strings from `fixtures/scenario2-debrief.json` verbatim in fenced code blocks labeled per-persona. Add one-line note: "Source: `.planning/phases/08-qa-credential-audit/08-02-DEBRIEF-export.md` lines 118-122, captured verbatim during v1.0 Scenario-2 live run 2026-04-15T01:24:56.435Z."
      - **§2b Preprocessor-expanded texts:** run each of the three persona texts through `preprocess()` from `backend/app/services/text_preprocessor.py` and embed the expanded output per persona. `EDIP` should expand to `E D I P` in all three. This proves the text that actually reached ElevenLabs.
      - **§2c Request bodies:** for each of the three POST segments, show the request body JSON with `xi-api-key: ***REDACTED***` in headers. Voice IDs in the `voice_id` field appear as `***REDACTED***`. The `text` field is the expanded text from §2b verbatim.

    - **§3 Raw response evidence per segment** — three subsections (Kent / Finch / Chen), each with the same shape:
      - HTTP status (expect 200)
      - `Content-Type` (expect `audio/mpeg` from the token pull; the SSE stream is `text/event-stream`)
      - `Content-Length` if present (note: ElevenLabs often omits this on streaming responses)
      - End-to-end latency (POST to segment complete)
      - First 32 bytes of segment as hex (sliced from stitched MP3 using offsets — see Task 2 script spec)
      - Any notable headers EXCLUDING the sensitive-header list (`xi-api-key`, `authorization`, `set-cookie`, etc.)

    - **§4 Stitched MP3 artifact** — table:
      - File: `evidence/debrief-scenario2-live.mp3` (relative link)
      - SHA-256: (from Task 2 stdout)
      - Size bytes: (from Task 2 stdout)
      - First 32 bytes hex: (frame-header proof)
      - Duration computed from CBR formula: `(size * 8) / 128000` seconds
      - Duration reported by VLC: (from operator Step 1)
      - Match: "Yes" if within 1s — document any variance as cosmetic (WMP quirk note from 14-03)
      - Offsets JSON: `evidence/segment-offsets.json` (relative link) — embed the `offsets` array inline

    - **§5 Verdict** — three parts:
      - **SC1 (stitched MP3 plays end-to-end):** PASS or FAIL with one-sentence justification from operator Step 1.
      - **SC2 (acronym pronunciation deviation table):** the 8-row markdown table with columns `term | expected pronunciation | heard | tier | disposition`. Double-rows for any Tier-A before/after entries. A one-paragraph overall tiered verdict ("SC2 PASS with 1 Tier-A fix applied in-phase and 2 Tier-B observations deferred to v1.3 VOICE-01" or similar).
      - **SC3 (evidence bundle completeness):** bullet checklist of: fixture ✓, replay script ✓, stitched MP3 ✓, offsets JSON ✓, player screenshot ✓, deviation table ✓.

    - **§6 Cross-references** — bullet list of relative links with anchors:
      - v1.1 Tier-B precedent: `../12-crisis-state-prompt-engineering/12-LIVE-VERIFICATION.md`
      - Phase 13 firewall evidence: `../13-firewall-spike-mockable-backend-foundation/13-VERIFICATION.md`
      - Phase 14 end-to-end-on-fake evidence: `../14-podcast-endpoint-player/14-VERIFICATION.md`
      - Phase 15 graceful-degradation evidence: `../15-tts-health-graceful-degradation/15-VERIFICATION-AUDIT.md` + `../15-tts-health-graceful-degradation/15-VERIFICATION.md`
      - v1.1 milestone-audit shape: `../../milestones/v1.1-MILESTONE-AUDIT.md`
      - Preprocessor golden corpus: `../../../backend/tests/fixtures/preprocessor_golden.json` + commit SHAs of any Tier-A dict updates made during this plan
      - REQUIREMENTS.md: `../../REQUIREMENTS.md` (referenced by the v1.2-MILESTONE-AUDIT.md in plan 16-02)

    **Redaction checklist Claude runs on the drafted file before saving:**
    - [ ] `grep -c "ELEVENLABS_API_KEY" 16-LIVE-VERIFICATION.md` matches only env-var NAME mentions, never a value.
    - [ ] No 20+-character alphanumeric strings that could be voice IDs outside of code fences labeled as `***REDACTED***` examples.
    - [ ] No `xi-api-key` header value written — always `***REDACTED***`.
    - [ ] No `Authorization:` header value written — always `***REDACTED***`.
    - [ ] `.env` contents NOT embedded.
    - [ ] No stdout fragment containing a raw voice ID copied in.

    **Resume signal:** Operator posts the deviation table (8+ rows) + VLC duration + SC1 verdict + SC2 per-row verdict + Task 2 replay-script stdout. Then Claude authors `16-LIVE-VERIFICATION.md` using the above spec, runs the redaction checklist, and commits it with message `docs(16-01): Tier-B live verification evidence record for v1.2 Phase 16`.

    **Tier-C escalation affordance:** If ANY deviation-table row is tier C OR SC1 verdict is FAIL, Claude does NOT author the evidence record; instead Claude halts and surfaces: "Tier-C defect detected: <details>. Milestone halted per 16-CONTEXT.md ship-criteria policy. Recommend Phase 17 insert to address. Do not proceed to plan 16-02."
  </how-to-verify>

  <resume-signal>
    Operator posts:
    1. The 8-row deviation table (plus any Tier-A before/after double-rows) with verdict per row.
    2. VLC-reported total MP3 duration.
    3. SC1 overall verdict (PASS / FAIL).
    4. SC2 tiered overall verdict ("PASS with N Tier-A fixes applied" or "PASS with N Tier-B deferrals" or "FAIL — Tier C").
    5. The captured replay-script stdout from Task 2 sub-step C step 5.
    6. Confirmation that `evidence/player-screenshot.png` has been saved to disk.

    Then type "approved" or "proceed to authoring".

    If ANY row is Tier C or SC1 = FAIL, operator posts "Tier C blocker — <description>" instead; Claude halts and does not author the evidence record.
  </resume-signal>
</task>

</tasks>

<verification>

**Plan-level verification (runs after all three tasks complete):**

1. **Ship gates from 16-CONTEXT.md "Milestone ship criteria" — evaluated in order:**
   - SC1 binary pass/fail: MP3 plays end-to-end in VLC with three distinct voices Kent → Finch → Chen at expected duration. VLC-reported duration documented in 16-LIVE-VERIFICATION.md §4.
   - SC2 tiered pass: all 8 acronym rows present in deviation table. Tier-A fixes applied in-phase and double-rowed. Tier-B deferrals have disposition `deferred-to-VOICE-01`. Zero Tier-C rows.
   - SC3 completeness: fixture ✓, replay script ✓, stitched MP3 ✓, offsets JSON ✓, player screenshot ✓, evidence record ✓ — all six present and committed.

2. **File-existence check:**
   - `ls .planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json` → exists
   - `ls .planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py` → exists
   - `ls .planning/phases/16-live-elevenlabs-verification/evidence/debrief-scenario2-live.mp3` → exists, size 80KB-1.2MB
   - `ls .planning/phases/16-live-elevenlabs-verification/evidence/segment-offsets.json` → exists, JSON-parses
   - `ls .planning/phases/16-live-elevenlabs-verification/evidence/player-screenshot.png` → exists, non-zero size
   - `ls .planning/phases/16-live-elevenlabs-verification/16-LIVE-VERIFICATION.md` → exists, 120+ lines, six sections, zero `[TBD]` placeholders
   - `ls backend/app/routers/config_tts.py backend/tests/test_config_tts.py src/lib/ttsVoicesClient.ts src/lib/__tests__/ttsVoicesClient.test.ts` → all four exist

3. **Redaction hygiene (global grep on phase directory):**
   - `grep -rE "ELEVENLABS_API_KEY=[a-zA-Z0-9]" .planning/phases/16-live-elevenlabs-verification/` → zero matches (env-var name mentions without `=value` are fine)
   - `grep -rE "xi-api-key: [a-zA-Z0-9]{10,}" .planning/phases/16-live-elevenlabs-verification/` → zero matches (only `***REDACTED***` allowed)
   - `grep -rE "voice_id.{0,5}[\"']([a-zA-Z0-9]{20,})[\"']" .planning/phases/16-live-elevenlabs-verification/` → zero matches outside `***REDACTED***` or `__fake_*__` contexts
   - `git check-ignore backend/.env` → reports `.env` is ignored (it must NOT be committed by this plan)

4. **Test-suite state (post-Task 1):**
   - Backend: `cd backend && pytest -q` → all green, count is prior-exit-count + ~3 (for test_config_tts.py).
   - Frontend: `npm run test -- --run` → all green, count is prior-exit-count + ~2 (for ttsVoicesClient.test.ts).
   - Frontend build: `npm run build` → succeeds.

5. **Key-links integrity:**
   - `grep -n "fetchTtsVoices" src/components/game/FacilitatorInput/ActionToolbar.tsx` → at least 2 matches (both in-session Generate path and force-fresh Re-generate path).
   - `grep -n "__fake_kent__\|__fake_finch__\|__fake_chen__" src/components/game/FacilitatorInput/ActionToolbar.tsx` → zero matches.
   - `grep -n "include_router(config_tts" backend/app/main.py` → exactly one match, line number < the `SPAStaticFiles` mount line.

6. **Evidence-record structure:**
   - 16-LIVE-VERIFICATION.md has sections 1-6 in order (regex `^## [1-6]\.`) with no missing numbers.
   - Section 5 contains a markdown table with exactly the columns `term | expected pronunciation | heard | tier | disposition` and at least 8 data rows (one per canonical acronym).
   - Section 6 contains relative-path links to 12/13/14/15 VERIFICATION files and v1.1-MILESTONE-AUDIT.md.

</verification>

<success_criteria>

This plan is complete when:

- [ ] `backend/app/routers/config_tts.py` exists, returns 200 for `GET /api/config/tts-voices` with the correct branch shape (fake sentinels vs real IDs), is registered in `main.py` before the SPA mount, has 3 passing pytest tests in `test_config_tts.py`.
- [ ] `src/lib/ttsVoicesClient.ts` exists, exports `fetchTtsVoices()`, has 2 passing vitest tests in `ttsVoicesClient.test.ts`.
- [ ] `src/components/game/FacilitatorInput/ActionToolbar.tsx` contains zero hardcoded sentinel voice ID strings; both Generate and Re-generate paths await `fetchTtsVoices()`.
- [ ] Full backend pytest suite is green; full frontend vitest + production build are green.
- [ ] `.planning/phases/16-live-elevenlabs-verification/fixtures/scenario2-debrief.json` is locked with the three Scenario-2 debrief persona texts verbatim from 08-02-DEBRIEF-export.md lines 118-122.
- [ ] `.planning/phases/16-live-elevenlabs-verification/scripts/run_live_replay.py` is committed, follows the `run_firewall_spike.py` auth-hygiene pattern, parses SSE inline without new deps beyond `requests`, contains an auth-leak assertion before exit, and supports single-persona re-gen via `--persona <name>` for Tier-A fix loops.
- [ ] `evidence/debrief-scenario2-live.mp3` is committed as binary, plays end-to-end in VLC, contains three distinct voices in Kent → Finch → Chen order with ~700ms pads between. VLC duration documented in 16-LIVE-VERIFICATION.md.
- [ ] `evidence/segment-offsets.json` matches the backend's `done` SSE event offsets and is consistent with the stitched MP3's CBR-computed segment boundaries.
- [ ] `evidence/player-screenshot.png` shows the PodcastPlayer in loaded-paused state with the `<audio controls>` element, three Skip buttons, Now-playing label, and Download MP3 button all visible — captured from a real ElevenLabs-path browser session that went through the new `/api/config/tts-voices` endpoint.
- [ ] `16-LIVE-VERIFICATION.md` has all six sections populated with no `[TBD]` placeholders, no API key or voice ID values leaked, and a full 8-row acronym deviation table with tiered verdicts per 16-CONTEXT.md.
- [ ] Every Tier-A row in the deviation table has a corresponding preprocessor dict entry + golden-corpus entry committed; `pytest tests/test_preprocessor.py -v` passes post-update.
- [ ] Zero Tier-C rows in the deviation table (if any existed, the plan halted per the escalation affordance in Task 3 and did not reach this state).
- [ ] Roadmap SC1 verdict = PASS, SC2 verdict ∈ {PASS, PASS-with-tier-A-fixes, PASS-with-tier-B-deferrals}, SC3 completeness = PASS.
- [ ] All files above committed; `backend/.env` and raw credentials remain gitignored and never committed.

</success_criteria>

<output>
After completion, create `.planning/phases/16-live-elevenlabs-verification/16-01-SUMMARY.md` following the standard summary template:

- Frontmatter: `phase`, `plan`, `shipped_date`, `status: complete`, `subsystem: evidence+frontend+backend`, `tech-stack.added: [config_tts_endpoint, live_replay_script]`, `affects: [16-02]`, `requires: [13-01, 14-*, 15-*]`.
- Key decisions (expect 3-5): why `/api/config/tts-voices` rather than baking voice IDs into a generic `/api/config` surface; whether Tier-A fixes were applied and which acronyms triggered them; any cosmetic variance between VLC and CBR-formula duration; any Tier-B deferrals captured into v1.3 VOICE-01 backlog.
- Key files: the six evidence artifacts + the three code files touched + the two new test files.
- Verification summary: SC1/SC2/SC3 verdicts with one-line justification each.
- Handoff to 16-02: explicit "the v1.2 milestone audit can cite this evidence record to validate PODGEN-01..08 and PODPLAY-01..05 against the real ElevenLabs endpoint; PODRES-01..03 evidence remains `15-VERIFICATION.md`; PODDEP-01..02 evidence remains `13-VERIFICATION.md`."
</output>
