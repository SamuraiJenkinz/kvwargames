---
phase: 08-qa-credential-audit
plan: 02
type: execute
wave: 2
depends_on: ["08-01", "08-03", "08-04", "08-05"]
files_modified:
  - .planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md
autonomous: false

must_haves:
  truths:
    - "08-02-LIVE-RUN.md exists and contains a per-round log for ALL FIVE rounds of Scenario 2 — partial or selective-round verdicts are NOT accepted (08-CONTEXT 'Sign-off rule')"
    - "Each round's entry records: facilitator input verbatim, persona responses (speaker + abbreviated text), reportPromptBudget output (withinLimit verdict), llmHistory.length per turn (must satisfy llmHistory.length <= 2*N+1 invariant where N=2)"
    - "Persona attribution check captured: every persona bubble's color-coded chip matches the routing expectation (Kent blue / Finch amber / Chen green per round role)"
    - "Round 4 and Round 5 specifically called out: persona voice has not drifted; JSON structure still parses; state values consistent with prior rounds"
    - "Network evidence captured: DevTools HAR export OR annotated screenshot of a POST /api/llm request — request headers contain NO Authorization, NO api-key, NO direct LLM endpoint URL (feeds 08-03 § Network Evidence)"
    - "Multi-trigger live assertion captured: one facilitator message combining round-start + card-play + dispute cues — observed response has 2-3 distinct speakers, no duplicates, additive (non-contradictory) state updates"
    - "Downloaded debrief markdown attached/linked — confirms Phase 7 export pipeline produces a clean artifact end-to-end with the 08-05 bucketing fix in place"
    - "One-paragraph facilitator verdict: PASS / FAIL / PASS-WITH-POLISH (the only three valid values per 08-CONTEXT)"
    - "Three ops-confirmation items resolved (or explicitly logged as unresolved): corporate context window size, corporate proxy hard timeout, API key rotation status"
  artifacts:
    - path: ".planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md"
      provides: "Single growing markdown artifact — per-round notes, downloaded debrief link, HAR/screenshot evidence, multi-trigger observation, ops confirmations, facilitator verdict"
  key_links:
    - from: "08-CONTEXT 'Live run scope & sign-off' section"
      to: "08-02-LIVE-RUN.md structure"
      via: "1:1 mapping — every telemetry item in CONTEXT has a section in the artifact"
      pattern: "Live run scope"
    - from: "08-RESEARCH 'Open Questions' (3 items)"
      to: "08-02-LIVE-RUN.md § Ops Confirmations"
      via: "facilitator confirms each item before or during the run"
      pattern: "Ops Confirmations"
    - from: "08-05 debrief bucketing fix"
      to: "downloaded debrief markdown attached to 08-02"
      via: "live-run debrief export must reflect the fix — no duplication of post-debrief persona messages in round transcripts"
      pattern: "Downloaded Debrief"
    - from: "08-03 credential audit § Network Evidence"
      to: "08-02-LIVE-RUN.md § Network Evidence"
      via: "08-03 cross-references this artifact for the HAR/screenshot"
      pattern: "Network Evidence"
---

<objective>
Run a full 5-round Scenario 2 live session against the real corporate LLM, capturing all required telemetry into a single growing markdown artifact `08-02-LIVE-RUN.md`. This is the ONLY plan in Phase 8 that requires a running backend + real LLM endpoint. It is the gating deliverable for phase completion: a failed live run does NOT ship.

Purpose:
- Phase 8 success criterion #1 (5-round Scenario 2 completes with correct persona voice, valid JSON, accurate state in R4/R5) is satisfied ONLY by a real run; mocking defeats the test.
- Phase 8 success criterion #2 (zero Authorization header on browser→backend requests) is captured here as DevTools network evidence — 08-03 cross-references it.
- Phase 8 success criterion #5 multi-trigger behavioural assertion is observed during this run — 08-05 captured the static-text-coverage half; this plan captures the live behaviour.
- Three ops-confirmation items from 08-RESEARCH Open Questions (corporate context window, proxy timeout, key rotation) are resolved or logged here.

Output: A single markdown file `.planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md` that a facilitator reading six months from now understands without DevTools access. Plus the downloaded debrief markdown (linked from the artifact).

This plan is `autonomous: false` — it requires the facilitator at the keyboard.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-qa-credential-audit/08-CONTEXT.md
@.planning/phases/08-qa-credential-audit/08-RESEARCH.md
@.planning/phases/08-qa-credential-audit/08-01-SUMMARY.md
@.planning/phases/08-qa-credential-audit/08-03-SUMMARY.md
@.planning/phases/08-qa-credential-audit/08-04-SUMMARY.md
@.planning/phases/08-qa-credential-audit/08-05-SUMMARY.md

# Codebase landmarks
@src/lib/promptBudget.ts
@src/lib/gameStore.ts
</context>

<decisions_locked>
1. **Live run depends on 08-01, 08-03, 08-04, 08-05 being green first**. Ordering rationale: a failure during the live run should be attributable to live-run conditions (LLM behaviour, prompt drift, network), not to a latent local-test gap or a known-buggy debrief exporter. With Wave 1 green, any defect surfaced is a real signal.
2. **Scenario 2 at FULL 5 rounds — non-negotiable** (08-CONTEXT). Maximum history depth is the point; truncated runs do not satisfy success criterion #1.
3. **Real corporate LLM, not mock** (08-CONTEXT). Mocking the run defeats the production-conditions test.
4. **Sign-off rule = single verdict at end** (08-CONTEXT): PASS / FAIL / PASS-WITH-POLISH. No partial verdicts. A FAIL means: fix the defect (within Phase 8 scope if it's a plan 08-0x issue; new phase if new capability) and re-run from the start of Round 1.
5. **Single markdown artifact** (08-CONTEXT specifics): not scattered screenshots. The artifact tells the story.
6. **Multi-trigger assertion is one specific facilitator message** crafted to combine three trigger cues (round-start + card-play + dispute). 08-RESEARCH gives the example shape: `"[ROUND_START Round 3] Supply disruption — Team A plays EMDR Card — Team B disputes the legitimacy ruling"`. Use that exact pattern (or close variant) on Round 3 of the run.
7. **Network evidence = HAR export OR annotated screenshot** (facilitator's choice). The artifact records WHICH was used and WHY (e.g. "HAR exported because session had >50 requests; key request annotated separately").
8. **Manual error-injection observation (secondary)**: per 08-CONTEXT "Error injection mechanism" — mid-response, briefly toggle WiFi off (or Chrome → Offline) ONCE during one of rounds 2-4. Observe red error bubble surfaces, session recovers on next message. Capture the moment in the per-round notes. This is supplementary to the automated tests in 08-04, not a primary deliverable.
9. **Three ops-confirmation items must be addressed before or during the run** (08-RESEARCH Open Questions):
   - **Context window**: read DevTools console for `[promptBudget]` lines each turn; verify `withinLimit: true`. If consistently `false` or borderline, capture actual numbers and flag for ceiling adjustment.
   - **Proxy timeout**: if requests time out, note wall-clock time. If consistent (<45s frontend timeout), suspect proxy.
   - **Key rotation**: confirm with operator that the key has been rotated per Phase 06-08 follow-up #7 BEFORE starting the run (08-RESEARCH Open Question 3 + 08-03 plan section 4).
10. **Downloaded debrief is attached / linked**, not pasted inline. The exporter produces a `.md` file; copy it into `.planning/phases/08-qa-credential-audit/` as `08-02-DEBRIEF-export.md` and link from the artifact. This proves end-to-end Phase 7 + 08-05 fix in the live pipeline.
</decisions_locked>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Pre-flight ops confirmations + scaffold the 08-02-LIVE-RUN.md artifact</name>
  <files>
    .planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md
  </files>
  <action>
    Before starting the live run, the facilitator MUST resolve three ops items (08-RESEARCH Open Questions 1-3). For each, ask the operator (or check the relevant config/dashboard) and record the answer in the scaffold:

    1. **API key rotation status** (08-RESEARCH Q3, 08-03 plan section 4 step 1):
       - Was the corporate LLM API key rotated per the Phase 06-08 follow-up #7 (key appeared in chat logs across two smoke tests)?
       - YES → record date of rotation; proceed.
       - NO → STOP. Rotate the key first (follow 08-03 § Key Rotation Checklist), then return to this task.

    2. **Corporate LLM context window size** (08-RESEARCH Q1):
       - Confirm with ops what the actual deployed model's context window is (the codebase assumes 8k via `SAFE_CONTEXT_CEILING_TOKENS = 7500`).
       - If >8k confirmed (e.g. 16k, 32k): record the actual value. After the run, raise `SAFE_CONTEXT_CEILING_TOKENS` and `HISTORY_WINDOW_N` accordingly and re-run the 06-08 budget measurement (this code change is Phase 9 backlog, NOT this plan).
       - If exactly 8k or unconfirmed: proceed with current settings; flag as "ASSUMED, NOT CONFIRMED" in the artifact.

    3. **Corporate proxy hard timeout** (08-RESEARCH Q2):
       - Confirm with ops what the proxy's hard request timeout is.
       - Compare against `LLM_FRONTEND_TIMEOUT_MS = 45000` and `LLM_TIMEOUT_SECONDS = 60`.
       - Record actual value; if proxy timeout < frontend timeout, log as a known constraint to watch for during the run.

    Then create the artifact scaffold at `.planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md`:

    ```markdown
    # Phase 08-02 Live Run — Scenario 2 (5 Rounds)

    **Run date:** YYYY-MM-DD
    **Facilitator:** <name>
    **Backend:** <commit SHA>
    **LLM:** corporate endpoint <model name if known>
    **Phase 8 dependencies green at run start:** 08-01 ✓ / 08-03 ✓ / 08-04 ✓ / 08-05 ✓ (cite SUMMARY files)

    ## Ops Confirmations (pre-flight)

    | Item | Status | Value | Notes |
    |------|--------|-------|-------|
    | API key rotated post-Phase 7 | YES / NO + date | — | (08-03 § Key Rotation Checklist) |
    | Corporate LLM context window | CONFIRMED / ASSUMED | <Nk> | (08-RESEARCH Q1) |
    | Corporate proxy hard timeout | CONFIRMED / ASSUMED | <N>s | (08-RESEARCH Q2) |

    ## Round 1
    *(populated during the run)*

    ## Round 2
    *(populated during the run — also record manual WiFi-off observation if done here)*

    ## Round 3 — Multi-Trigger Test
    *(populated during the run — facilitator message combines round-start + card-play + dispute cues)*

    ## Round 4
    *(populated during the run — explicit persona-voice check)*

    ## Round 5
    *(populated during the run — explicit persona-voice check; downloaded debrief at end)*

    ## Network Evidence
    *(HAR export or annotated screenshot of POST /api/llm — confirms zero Authorization header on browser side; cross-referenced from 08-03)*

    ## Downloaded Debrief
    *(link to attached markdown — confirms 08-05 bucketing fix in live pipeline)*

    ## Facilitator Verdict

    **Verdict:** PASS / FAIL / PASS-WITH-POLISH

    *(one paragraph: what worked, what didn't, any polish items deferred to next milestone)*
    ```

    Once the scaffold exists and ops items are recorded, the facilitator is ready to begin the run. STOP here for the human to drive Tasks 2-4.
  </action>
  <verify>
    - File `.planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md` exists with the scaffold above
    - Ops Confirmations table has values filled in (not placeholder dashes) OR explicit "ASSUMED" markers
    - All four Wave 1 SUMMARY files (08-01, 08-03, 08-04, 08-05) exist and are cited in the Dependencies line — if any is missing, STOP (Wave 1 incomplete)
  </verify>
  <done>
    Pre-flight ops items addressed (key rotation confirmed; context window + proxy timeout recorded). Artifact scaffold written. Ready for the live run.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Run all 5 rounds of Scenario 2 against the real LLM; populate per-round entries in the artifact</name>
  <files>
    .planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md
  </files>
  <action>
    Start the backend and frontend dev servers. Open the app, load Scenario 2 (full EDIP config), and begin the run.

    For EACH ROUND (1 through 5), capture the following in the corresponding `## Round N` section of the artifact:

    **Per-round telemetry template:**
    ```markdown
    ### Round N

    **Facilitator inputs (in order):**
    - Turn 1: "<verbatim text>"
    - Turn 2: "<verbatim text>"
    *(etc.)*

    **Persona responses (speaker + summary):**
    - Kent (blue chip): <one-line summary; quote anything unusual>
    - Finch (amber chip): <one-line summary>
    - Chen (green chip): <one-line summary>

    **Telemetry per turn:**
    | Turn | reportPromptBudget withinLimit | llmHistory.length | Notes |
    |------|-------------------------------|-------------------|-------|
    | 1    | true / false (cite numbers)   | <N>               | invariant: N <= 2*ROUND+1 |
    | 2    | ...                           | ...               | ... |

    **Persona-attribution check:** PASS / FAIL — every bubble color-coded correctly?

    **Anomalies:** none / <describe>
    ```

    **Round 3 special**: include the multi-trigger facilitator message (08-RESEARCH "Multi-trigger persona routing"). Use a message like:
    > `"[ROUND_START Round 3] Supply disruption emerging — Team A plays EMDR Coordination Card — Team B disputes the legitimacy of Team A's framing"`

    Then explicitly assert in the round notes:
    - Number of distinct speakers in the response (target: 2-3)
    - No speaker appears twice
    - State updates from each persona are additive, not contradictory (e.g. one persona changes Team A's PC, another changes crisisSeverity — not both setting Team A's PC to conflicting values)

    **Round 4 special**: explicitly check persona voice against Round 1 — has Kent's tone shifted? Has Finch started using Kent's vocabulary? Capture a one-liner.

    **Round 5 special**: same voice check vs Round 1. Then trigger a debrief and download the markdown export.

    **Manual error-injection observation (08-CONTEXT secondary mechanism)**: pick ONE round (recommend Round 2 or 3, not 5) and mid-response, toggle WiFi off briefly. Observe:
    - Red error bubble appears
    - Session does NOT crash
    - Re-enabling WiFi + sending the next facilitator message produces a valid response
    Capture in that round's "Anomalies" section as "manual offline injection — recovered cleanly" (or whatever was observed).

    **DevTools open throughout**: keep the Network tab and Console open. Console captures `[promptBudget]` lines each turn (paste into the per-turn telemetry table). Network tab is needed for Task 3.

    If at any point the run becomes invalid (e.g. browser crash, backend crash, LLM consistently failing), STOP, capture the failure in the artifact, and the verdict is FAIL. Re-run from Round 1 after fixing the defect.
  </action>
  <verify>
    - All five `## Round N` sections in the artifact have populated content (no `*(populated during the run)*` placeholders left)
    - Each round has at least one row in the per-turn telemetry table
    - Round 3 entry explicitly addresses the multi-trigger message + 2-3 distinct speakers + no duplicates
    - Rounds 4 and 5 have explicit persona-voice notes
    - Round 5 entry includes a debrief download (download triggered, .md file produced)
    - At least one round has a manual offline-injection note
    - `llmHistory.length` invariant verified each turn (recorded value <= 2*ROUND+1; with HISTORY_WINDOW_N=2 the cap is 5 from round ≥ 2)
  </verify>
  <done>
    Five full rounds of Scenario 2 completed against the real LLM. All telemetry captured. Multi-trigger assertion observed in Round 3. Persona-voice consistency check captured in Rounds 4 and 5. Debrief downloaded at end of Round 5. Manual offline injection captured in one round. Any anomalies recorded.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Capture network evidence + attach downloaded debrief + write facilitator verdict</name>
  <files>
    .planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md
    .planning/phases/08-qa-credential-audit/08-02-DEBRIEF-export.md
  </files>
  <action>
    **Network evidence (08-CONTEXT § Credential audit depth, 08-03 § Network Evidence cross-reference)**:

    With DevTools Network tab still open from Task 2, locate any `POST /api/llm` request from the run (any round will do — they're all browser→backend).

    Choose ONE of:
    - **Option A — HAR export**: Network tab → Right-click → Save all as HAR with content. Save as `.planning/phases/08-qa-credential-audit/08-02-network.har`. Then in the artifact's `## Network Evidence` section, paste the relevant request's headers excerpt:
      ```
      Request URL: http://localhost:5173/api/llm  (or whatever the dev origin is)
      Request Method: POST
      Request Headers:
        accept: */*
        content-type: application/json
        (NO Authorization, NO api-key, NO Bearer)
      ```
    - **Option B — Annotated screenshot**: take a screenshot of the request's Headers panel. Save as `.planning/phases/08-qa-credential-audit/08-02-network-headers.png`. Annotate (in image or in text) which fields are absent (Authorization, api-key, Bearer).

    Either way, include a one-line verdict in the artifact: "Confirmed: zero Authorization, zero api-key, zero direct LLM endpoint URL on browser-originated POST /api/llm requests. Backend adds upstream auth headers server-side only (cf. backend/tests/test_llm_auth_header.py)."

    **Pitfall awareness (08-RESEARCH Pitfall 5)**: looking at browser→backend requests. The browser request to `localhost:5173/api/llm` (or similar) MUST NOT have auth headers — the backend adds them on the OUTBOUND side. If you see Authorization on the browser request, that is a CRITICAL FINDING and the verdict is FAIL.

    **Attach downloaded debrief**:
    - Take the `.md` file the user downloaded at the end of Round 5 (default filename pattern: `debrief-edip-security-of-supply-wargame-YYYY-MM-DD-HHMM.md` per Phase 7).
    - Copy it into `.planning/phases/08-qa-credential-audit/08-02-DEBRIEF-export.md` (rename for canonical location; the original timestamped file can be discarded or kept as a backup).
    - In the artifact's `## Downloaded Debrief` section, add a link: `See [08-02-DEBRIEF-export.md](./08-02-DEBRIEF-export.md) for the full export.`
    - Verify by inspection: the debrief contains all 6 required Phase 7 sections (H1, metadata, per-round, ## Debrief, ## Final State, ## Appendix). And specifically: the Round-N transcripts do NOT duplicate the post-debrief persona messages (08-05 fix proven in live pipeline).

    **Facilitator Verdict** (one paragraph):

    Pick exactly one of: **PASS** / **FAIL** / **PASS-WITH-POLISH**.

    Then a short paragraph (3-5 sentences) covering:
    - Did persona voice hold across all 5 rounds?
    - Did JSON parse cleanly every turn?
    - Did the multi-trigger Round 3 assertion succeed?
    - Did the manual offline injection recover cleanly?
    - Were there any anomalies that, while not failing the run, are worth logging as next-milestone polish items?

    If the verdict is FAIL: do NOT close the artifact. Leave the verdict section as FAIL with the failure mode named. The run must be repeated after the defect is fixed (within Phase 8 scope if it's a plan 08-0x issue; new phase if it's a new capability per 08-CONTEXT sign-off rule).

    If the verdict is PASS or PASS-WITH-POLISH: the artifact is complete. Move to plan 08-02 SUMMARY creation.
  </action>
  <verify>
    - `## Network Evidence` section is populated with HAR excerpt or screenshot reference + the absence-of-headers verdict line
    - `08-02-network.har` OR `08-02-network-headers.png` exists in the phase directory
    - `08-02-DEBRIEF-export.md` exists in the phase directory and contains the 6 Phase 7 sections
    - Manual inspection of the debrief: Round-N transcripts do NOT contain post-debrief persona messages (proves 08-05 fix worked end-to-end)
    - `## Facilitator Verdict` section contains exactly one of "PASS" / "FAIL" / "PASS-WITH-POLISH" + a paragraph
    - If FAIL: a defect is named and the artifact records that re-run is required
  </verify>
  <done>
    Network evidence captured with one-line verdict. Debrief markdown attached. Facilitator verdict written with one of three valid values. If verdict is PASS or PASS-WITH-POLISH, Phase 8 is ready to close. If FAIL, the failure mode is documented and re-run is queued.
  </done>
</task>

</tasks>

<verification>
1. File `.planning/phases/08-qa-credential-audit/08-02-LIVE-RUN.md` exists and is fully populated (no `*(populated during the run)*` placeholders)
2. All five `## Round N` sections have content with per-turn telemetry tables
3. Round 3 explicitly addresses the multi-trigger assertion (2-3 distinct speakers, no duplicates, additive state)
4. Rounds 4 and 5 have persona-voice consistency notes vs Round 1
5. Network evidence captured (HAR or screenshot) with absence-of-headers verdict
6. `08-02-DEBRIEF-export.md` exists and contains all 6 Phase 7 sections + no post-debrief duplication in round transcripts
7. Manual offline-injection observation captured in at least one round
8. Ops Confirmations table is filled (key rotation YES/NO+date; context window + proxy timeout values or explicit ASSUMED markers)
9. Facilitator Verdict = PASS / FAIL / PASS-WITH-POLISH (one of exactly three values), with a paragraph
10. If verdict is FAIL: failure mode named, re-run required, Phase 8 not closed until re-run produces PASS or PASS-WITH-POLISH
</verification>

<success_criteria>
- A full, real, 5-round Scenario 2 live run completed against the corporate LLM
- All four Phase 8 telemetry items captured per turn (reportPromptBudget verdict, llmHistory.length invariant, persona attribution, voice consistency at R4/R5)
- Phase 8 success criterion #1 satisfied by the run
- Phase 8 success criterion #2 network evidence captured (cross-referenced into 08-03)
- Phase 8 success criterion #5 multi-trigger behavioural half captured in Round 3 (paired with 08-05's static-text half)
- Three Open Questions resolved or explicitly logged as ASSUMED with action item
- Downloaded debrief proves end-to-end Phase 7 + 08-05 fix in live pipeline
- Facilitator verdict is one of three valid values (no partial verdicts)
- Manual offline-injection observation captured (08-CONTEXT secondary mechanism)
</success_criteria>

<output>
After completion, create `.planning/phases/08-qa-credential-audit/08-02-SUMMARY.md` with:
- Final verdict (PASS / FAIL / PASS-WITH-POLISH)
- Run date, facilitator, backend SHA, LLM identity
- Per-round summary line (e.g. "R1: 2 turns, withinLimit:true, no anomalies")
- Multi-trigger Round 3 outcome (speaker count, duplicate check)
- Voice-consistency outcome (R1 vs R4 vs R5)
- Manual offline-injection outcome
- Ops items resolved (key rotation, context window, proxy timeout — values + actions for next milestone if any)
- Cross-references to 08-02-LIVE-RUN.md, 08-02-DEBRIEF-export.md, network evidence file
- Polish items deferred to next milestone backlog (if any)
</output>
