---
phase: 05-game-screen-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mocks/mockGameState.ts
  - src/mocks/seedMockState.ts
autonomous: true

must_haves:
  truths:
    - "In DEV mode, navigating to /game with gameState === null auto-seeds mock state and renders the screen without redirecting to /setup"
    - "Mock state exercises every visual: Round 2 mid-game, crisisSeverity=3, edipLegitimacy=+1, one STRAINED team (pc=1), one CRISIS team (pc=0), and a messages[] array with one of each type (round_divider x2, persona x3+ across kent/finch, facilitator, error)"
    - "Mock messages[] after the last round_divider contain kent and finch persona messages but NO chen message — so persona dots render 2 lit / 1 dim"
    - "store.loading === true after seed so the persona-attributed loading indicator renders at feed tail"
    - "Production build (import.meta.env.DEV === false) never seeds — /game with null gameState still redirects to /setup"
  artifacts:
    - path: "src/mocks/mockGameState.ts"
      provides: "MOCK_GAME_STATE (typed GameState) + MOCK_MESSAGES (typed ChatMessage[])"
      min_lines: 50
    - path: "src/mocks/seedMockState.ts"
      provides: "seedMockState() function that populates store with mock data + sets loading=true"
      min_lines: 15
  key_links:
    - from: "src/mocks/seedMockState.ts"
      to: "src/lib/gameStore.ts"
      via: "useGameStore.getState() — calls setGameConfig, setGameState, addMessages, setLoading"
      pattern: "useGameStore\\.getState\\(\\)"
    - from: "src/mocks/mockGameState.ts"
      to: "src/types/game.ts"
      via: "type imports for GameState, ChatMessage"
      pattern: "import.*from '@/types/game'"
---

<objective>
Create the dev-only mock state module and the seeder function that Plan 05-03 (GameScreen shell) will wire into the `/game` route mount path. Every visual in Phase 5 depends on this mock data — get it exhaustive and type-clean so no downstream plan has to shim.

Purpose: Decouple game screen development from the real Launch-from-setup flow. A dev can navigate straight to `/game` and see the full UI against a mid-game Round 2 snapshot.
Output: Two files in a new `src/mocks/` directory — one data module, one seeder function. Neither is imported by production code paths.
</objective>

<execution_context>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-screen-layout/05-CONTEXT.md
@.planning/phases/05-game-screen-layout/05-RESEARCH.md

# Source files the mock must match
@src/types/game.ts
@src/lib/gameStore.ts
@src/data/edipConfig.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock data module</name>
  <files>src/mocks/mockGameState.ts</files>
  <action>
Create `src/mocks/mockGameState.ts` exporting two named constants:

1. `MOCK_GAME_STATE: GameState` — Round 2 mid-game snapshot:
   - `round: 2`
   - `scenarioIndex: 0`
   - `crisisSeverity: 3`
   - `crisisState: 'Supply Crisis'`
   - `edipLegitimacy: 1`
   - `cardsThisRound: []`
   - `teams`: 4 entries matching `TeamState`:
     - Team A `{ id: 'A', name: 'Team A: Frontline & High-Threat', pc: 1, po: 0, readiness: 3, stock: 2, crm: 2, ic: 2 }` — STRAINED
     - Team B `{ id: 'B', name: 'Team B: Industrial Powerhouses', pc: 0, po: 1, readiness: 3, stock: 3, crm: 2, ic: 5 }` — CRISIS
     - Team C `{ id: 'C', name: 'Team C: Rear Support & Logistics', pc: 3, po: 0, readiness: 3, stock: 3, crm: 3, ic: 3 }`
     - Team D `{ id: 'D', name: 'Team D: Balancing / Mixed-Interest', pc: 4, po: 1, readiness: 3, stock: 3, crm: 3, ic: 3 }`

2. `MOCK_MESSAGES: ChatMessage[]` — at least one of every `MessageType`. Order matters (Round 1 block, then Round 2 block). Use plain string timestamps like `'10:01'`:
   - `{ id: 'm1', type: 'round_divider', label: 'Round 1', timestamp: '10:00' }`
   - `{ id: 'm2', type: 'persona', speaker: 'kent', text: '[Round 1 framing — CRM shortage identified; Team A should prioritise front-line stockpile preservation before considering transfers.]', timestamp: '10:01' }`
   - `{ id: 'm3', type: 'persona', speaker: 'finch', text: '[Round 1 economic analysis — industrial capacity strained at Team B; legitimacy cost of forced transfers is high this round.]', timestamp: '10:02' }`
   - `{ id: 'm4', type: 'persona', speaker: 'chen', text: '[Round 1 procedural note — no EDIP mechanism permits unilateral reallocation; voting rule applies.]', timestamp: '10:03' }`
   - `{ id: 'm5', type: 'facilitator', speaker: 'facilitator', text: 'Teams, please declare your national actions for Round 1.', timestamp: '10:05' }`
   - `{ id: 'm6', type: 'error', text: 'LLM timeout — response was not received. Please retry.', timestamp: '10:06' }`
   - `{ id: 'm7', type: 'debrief_divider', label: 'DEBRIEF', timestamp: '10:10' }`  (preview/demo — per CONTEXT.md the default mock includes this to exercise the visual)
   - `{ id: 'm8', type: 'round_divider', label: 'Round 2', timestamp: '10:15' }`
   - `{ id: 'm9', type: 'persona', speaker: 'kent', text: '[Round 2 escalation response — severity tracker has advanced; recommend Prioritisation (Soft) cards this round.]', timestamp: '10:16' }`
   - `{ id: 'm10', type: 'persona', speaker: 'finch', text: '[Round 2 industry position — CRM stockpiles sufficient for two rounds without emergency measures.]', timestamp: '10:17' }`
   - (NO chen message after m8 — this is intentional; persona indicator dots must render 2 lit / 1 dim for chen)

Import types from `@/types/game` using type-only imports. Export both constants as named exports.

Requirements:
- Must type-check against `GameState` and `ChatMessage[]` (run `tsc --noEmit`).
- No hardcoded hex colours, no JSX — pure data.
- Add a file-level JSDoc comment explaining this is a dev-only mock for Phase 5 rendering.
  </action>
  <verify>
1. `npx tsc --noEmit` passes with no new errors
2. `grep -n "MOCK_GAME_STATE\|MOCK_MESSAGES" src/mocks/mockGameState.ts` returns both export lines
3. Count of `type: 'persona'` entries ≥ 5 (kent x2, finch x2, chen x1)
4. Exactly one entry with `type: 'error'`, one with `type: 'debrief_divider'`, two with `type: 'round_divider'`
  </verify>
  <done>
File exists, type-checks cleanly, contains all message types and both STRAINED+CRISIS team cases in MOCK_GAME_STATE.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create seeder function</name>
  <files>src/mocks/seedMockState.ts</files>
  <action>
Create `src/mocks/seedMockState.ts` exporting a single function `seedMockState(): void` that:

1. Imports `useGameStore` from `@/lib/gameStore`.
2. Imports `EDIP_CONFIG` from `@/data/edipConfig`.
3. Imports `MOCK_GAME_STATE` and `MOCK_MESSAGES` from `./mockGameState`.
4. Calls `useGameStore.getState()` once and destructures `setGameConfig`, `setGameState`, `addMessages`, `setLoading`.
5. Invokes in order:
   - `setGameConfig(EDIP_CONFIG as GameConfig)` — use a type assertion since EDIP_CONFIG is `as const satisfies GameConfig`.
   - `setGameState(MOCK_GAME_STATE)`
   - `addMessages(MOCK_MESSAGES)`
   - `setLoading(true)` — so the persona-attributed loading indicator renders at feed tail on first paint.

Add a file-level JSDoc comment stating this is dev-only and must not be called from production code paths. Do NOT add `import.meta.env.DEV` guarding inside this function — that gate lives at the call site in Plan 05-03 (GuardedGameScreen). Keeping this function pure and unconditional makes it testable and keeps the gate in one visible place.

Import `GameConfig` as a type-only import from `@/types/game`.
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. `grep -n "seedMockState" src/mocks/seedMockState.ts` shows the exported function
3. File contains calls to `setGameConfig`, `setGameState`, `addMessages`, `setLoading`
4. No `import.meta.env.DEV` reference inside this file (gate lives at call site)
  </verify>
  <done>
`seedMockState()` is exported, type-safe, idempotent when called multiple times (overwrites state), and contains no environment gating.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit` passes
- Both files exist under `src/mocks/`
- No production file yet imports these (call-site wiring is Plan 05-03)
- `grep -rn "src/mocks" src/` returns only the two new files (no premature wiring)
</verification>

<success_criteria>
- `src/mocks/mockGameState.ts` exports `MOCK_GAME_STATE` and `MOCK_MESSAGES` that type-check and exercise every MessageType + both PC badge variants
- `src/mocks/seedMockState.ts` exports `seedMockState()` that populates the store via public actions + sets loading=true
- Zero new TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-game-screen-layout/05-01-SUMMARY.md`
</output>
